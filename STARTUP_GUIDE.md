# 🚀 Руководство по запуску проекта

## 📋 Первоначальная настройка

### 1. Установка зависимостей

Из корня проекта:
```bash
cd /Users/who/гробы
pnpm install
pnpm approve-builds  # Разрешить выполнение build скриптов
```

### 2. Настройка базы данных

```bash
cd apps/api
npx prisma generate
npx prisma migrate dev
```

## ▶️ Запуск проекта

### Из корня проекта (`/Users/who/гробы`):

```bash
# API сервер
pnpm dev:api

# Магазин (Web)
pnpm dev:web

# Админ-панель
pnpm dev:admin

# Всё сразу
pnpm dev:all
```

### Из отдельных директорий:

**API:**
```bash
cd /Users/who/гробы/apps/api
pnpm start:dev
```

**Web (магазин):**
```bash
cd /Users/who/гробы/apps/web
pnpm dev
```

**Admin (админ-панель):**
```bash
cd /Users/who/гробы/apps/admin
pnpm dev
```

## ⚠️ Важно!

1. **Пути:** Если вы находитесь в `apps/api`, то для перехода в другие директории используйте:
   ```bash
   cd ../web      # вместо cd apps/web
   cd ../admin    # вместо cd apps/admin
   ```

2. **Зависимости:** После клонирования репозитория всегда выполняйте `pnpm install` из корня проекта.

3. **Build скрипты:** Если видите предупреждение о build scripts, выполните `pnpm approve-builds`.

## 🔧 Устранение проблем

### `nest: command not found`
```bash
cd /Users/who/гробы
pnpm install
cd apps/api
pnpm add -D @nestjs/cli
```

### Ошибки TypeScript в IDE
1. Перезапустите TypeScript сервер: `Cmd+Shift+P` → `TypeScript: Restart TS Server`
2. Или перезапустите IDE

### База данных не найдена
```bash
cd apps/api
npx prisma migrate dev
npx prisma generate
```

## 📍 Порты

- **API:** http://localhost:3000
- **Web:** http://localhost:5173 (или другой порт Vite)
- **Admin:** http://localhost:5174 (или другой порт Vite)

