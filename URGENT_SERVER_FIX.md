# 🚨 СРОЧНО: Сервер перегружен - что делать?

## Текущая ситуация

✅ **Ping работает** - сервер не упал полностью  
❌ **SSH не отвечает** - сервер перегружен из-за Docker сборки

---

## ⚡ Немедленные действия

### Вариант 1: Подождать (РЕКОМЕНДУЕТСЯ)

Docker сборка может занять **5-10 минут**. Просто подождите:

```bash
# Проверяйте каждые 2-3 минуты
ping -c 1 94.241.141.194

# Пробуйте подключиться
ssh root@94.241.141.194
```

**Подождите еще 5-10 минут** - сборка должна завершиться.

---

### Вариант 2: Перезагрузка через панель управления

Если у вас есть доступ к панели управления хостингом:

1. Зайдите в панель управления
2. Найдите сервер `94.241.141.194`
3. Нажмите "Перезагрузка" / "Reboot"
4. Подождите 2-3 минуты
5. Подключитесь снова

---

### Вариант 3: VNC/KVM консоль

Если есть доступ к консоли:

1. Подключитесь к VNC/KVM
2. Нажмите `Ctrl+Alt+F2` (или другую консоль)
3. Войдите как root
4. Выполните:
   ```bash
   # Остановите Docker сборку
   pkill -9 docker
   
   # Очистите процессы
   docker stop $(docker ps -q) 2>/dev/null || true
   
   # Перезагрузите
   reboot
   ```

---

## 🔧 После восстановления доступа

### 1. Проверьте ресурсы

```bash
ssh root@94.241.141.194

# Память
free -h

# Диск
df -h

# Процессы
top
```

### 2. Очистите Docker

```bash
cd /opt/ritual-app

# Остановите все
docker-compose -f docker-compose.production.yml down

# Очистите неиспользуемое
docker system prune -a --volumes -f

# Проверьте место
df -h
```

### 3. Соберите локально (РЕКОМЕНДУЕТСЯ)

**На вашей локальной машине:**

```bash
cd /Users/who/гробы

# Соберите образы локально
docker build -t ritual-api:latest -f apps/api/Dockerfile .
docker build -t ritual-web:latest -f apps/web/Dockerfile .

# Сохраните в файлы
docker save ritual-api:latest | gzip > ritual-api.tar.gz
docker save ritual-web:latest | gzip > ritual-web.tar.gz

# Загрузите на сервер (после восстановления доступа)
scp ritual-api.tar.gz ritual-web.tar.gz root@94.241.141.194:/tmp/
```

**На сервере:**

```bash
ssh root@94.241.141.194

# Загрузите образы
docker load < /tmp/ritual-api.tar.gz
docker load < /tmp/ritual-web.tar.gz

# Измените docker-compose.production.yml
cd /opt/ritual-app
nano docker-compose.production.yml
```

Измените:

```yaml
# Было:
api:
  build:
    context: .
    dockerfile: apps/api/Dockerfile

# Стало:
api:
  image: ritual-api:latest
```

И то же для `web`.

Затем:

```bash
# Запустите без сборки
docker-compose -f docker-compose.production.yml up -d
```

---

## 📊 Почему это произошло?

1. **Первая сборка** - нет кеша, все компилируется
2. **Два образа одновременно** - web и api собираются параллельно
3. **TypeScript компиляция** - очень ресурсоемкая
4. **Недостаточно RAM** - сервер перегружен

---

## ✅ Предотвращение в будущем

### 1. Собирайте локально

Всегда собирайте образы на локальной машине и загружайте на сервер.

### 2. Добавьте ограничения ресурсов

В `docker-compose.production.yml` добавьте:

```yaml
services:
  api:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
```

### 3. Собирайте по очереди

Используйте обновленный `deploy.sh` - он собирает сервисы по очереди.

---

## 🆘 Если ничего не помогает

1. Обратитесь в поддержку хостинг-провайдера
2. Попросите перезагрузить сервер
3. Проверьте лимиты ресурсов в панели управления

---

## ⏱️ Текущий статус

- **Время ожидания**: 5-10 минут
- **Что происходит**: Docker собирает образы
- **Что делать**: Подождать или перезагрузить через панель

**Сервер не сломан, просто перегружен!** 🚀


