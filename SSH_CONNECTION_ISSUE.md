# 🔧 Проблема с SSH подключением

## Проблема

```
ssh: connect to host 94.241.141.194 port 22: Connection refused
```

**Причина:** SSH сервер не отвечает на порту 22, либо порт заблокирован firewall.

## Возможные причины

1. **SSH сервер не запущен** на сервере
2. **Firewall блокирует порт 22**
3. **SSH сервер слушает на другом порту**
4. **Проблемы с сетью/провайдером**

## Решение

### Вариант 1: Проверка через панель управления сервера

Если у вас есть доступ к панели управления хостинг-провайдера (VPS панель, Cloud панель и т.д.):

1. Зайдите в панель управления
2. Найдите раздел "SSH" или "Доступ"
3. Убедитесь что SSH включен
4. Проверьте настройки firewall - порт 22 должен быть открыт

### Вариант 2: Использование альтернативного доступа

Если есть:
- **VNC консоль** в панели управления
- **Web терминал** в панели управления
- **Другой способ доступа** к серверу

Используйте их для проверки SSH:

```bash
# На сервере проверьте статус SSH
systemctl status sshd
# или
systemctl status ssh

# Если не запущен, запустите
systemctl start sshd
systemctl enable sshd

# Проверьте что порт слушается
netstat -tlnp | grep :22
# или
ss -tlnp | grep :22

# Проверьте firewall
ufw status
# или
iptables -L -n | grep 22
```

### Вариант 3: Проверка через другой порт

Возможно SSH настроен на другой порт:

```bash
# Попробуйте стандартные альтернативные порты
ssh -p 2222 root@94.241.141.194
ssh -p 22022 root@94.241.141.194
```

### Вариант 4: Временное решение - деплой без SSH

Пока SSH не работает, можно использовать другие методы:

1. **Через панель управления** - загрузите файлы через веб-интерфейс
2. **Через FTP/SFTP** - если доступен
3. **Через Git** - если настроен git на сервере

## Проверка доступности сервера

Сайт работает по HTTP (порт 80), значит сервер доступен. Проблема именно с SSH.

```bash
# Проверка доступности сервера
ping 94.241.141.194

# Проверка HTTP (работает)
curl -I http://94.241.141.194

# Проверка SSH (не работает)
nc -zv 94.241.141.194 22
```

## Что делать дальше

1. **Свяжитесь с поддержкой хостинг-провайдера** - попросите проверить SSH доступ
2. **Используйте панель управления** для доступа к серверу
3. **Проверьте настройки firewall** в панели управления
4. **Попробуйте альтернативные порты** SSH

## Временное решение для деплоя

Пока SSH не работает, можно:

1. Использовать **веб-интерфейс панели управления** для загрузки файлов
2. Использовать **FTP/SFTP клиент** если доступен
3. Дождаться восстановления SSH доступа

---

**Важно:** Если сайт работает, значит сервер в порядке. Проблема только с SSH доступом, что не критично для работы сайта, но мешает деплою.


