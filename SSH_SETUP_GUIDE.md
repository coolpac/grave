# 🔐 Настройка SSH доступа

## Текущий статус

SSH ключ найден, но не настроен для автоматического подключения к серверу.

## Быстрая настройка

### Вариант 1: Автоматическая настройка (РЕКОМЕНДУЕТСЯ)

```bash
./setup-ssh.sh
```

Скрипт автоматически:
- ✅ Проверит наличие SSH ключа
- ✅ Добавит конфигурацию в `~/.ssh/config`
- ✅ Попытается скопировать ключ на сервер
- ✅ Протестирует подключение

### Вариант 2: Ручная настройка

#### 1. Добавьте конфигурацию в `~/.ssh/config`:

```bash
cat >> ~/.ssh/config << 'EOF'

# Ritual App Production Server
Host ritual-server
    HostName 94.241.141.194
    User root
    IdentityFile ~/.ssh/id_ed25519
    ServerAliveInterval 60
    ServerAliveCountMax 3
EOF
```

#### 2. Скопируйте SSH ключ на сервер:

```bash
# Автоматически (потребуется ввести пароль один раз)
ssh-copy-id -i ~/.ssh/id_ed25519.pub root@94.241.141.194

# Или вручную:
cat ~/.ssh/id_ed25519.pub | ssh root@94.241.141.194 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys"
```

#### 3. Проверьте подключение:

```bash
ssh ritual-server "echo 'SSH работает!'"
```

## Использование

После настройки вы можете подключаться просто:

```bash
# Вместо: ssh root@94.241.141.194
ssh ritual-server
```

Или использовать в скриптах:

```bash
ssh ritual-server "cd /opt/ritual-app && docker-compose ps"
```

## Обновление скриптов деплоя

Все скрипты деплоя (`deploy.sh`, `quick-deploy.sh`, и т.д.) уже используют правильный IP и пользователя, но после настройки SSH вы можете изменить их для использования алиаса `ritual-server` вместо `root@94.241.141.194`.

---

**Готово!** Теперь SSH доступ настроен и работает автоматически.


