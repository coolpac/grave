# 📋 Как скопировать setup-server.sh на сервер

## Вариант 1: Через scp (с локальной машины)

**В новом терминале на вашей локальной машине:**

```bash
# Из директории проекта
cd /Users/who/гробы

# Скопируйте скрипт на сервер
scp setup-server.sh root@94.241.141.194:/root/

# Сделайте его исполняемым
ssh root@94.241.141.194 "chmod +x /root/setup-server.sh"
```

## Вариант 2: Создать скрипт прямо на сервере

**В терминале где вы подключены к серверу (ssh root@94.241.141.194):**

```bash
# Создайте файл
nano setup-server.sh
```

Затем вставьте содержимое файла `setup-server.sh` (скопируйте весь файл) и сохраните (Ctrl+O, Enter, Ctrl+X).

После этого:
```bash
chmod +x setup-server.sh
./setup-server.sh
```

## Вариант 3: Через cat (быстрый способ)

**На локальной машине выполните:**

```bash
cat setup-server.sh | ssh root@94.241.141.194 "cat > /root/setup-server.sh && chmod +x /root/setup-server.sh"
```

---

## После копирования

**На сервере выполните:**

```bash
./setup-server.sh
```

Скрипт автоматически:
- Обновит систему
- Установит Docker и Docker Compose
- Настроит firewall
- Создаст необходимые директории
- Настроит автоматические backup

