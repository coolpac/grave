# ✅ Админка настроена и запущена

## 🌐 Адрес админки:

```
http://localhost:5174
```

## ⚠️ ВАЖНО:

**НЕ открывайте `http://localhost:5173`** - это магазин!

**Откройте `http://localhost:5174`** - это админка!

## 🔧 Что было исправлено:

1. ✅ Настроен отдельный порт `5174` для админки
2. ✅ Настроен прокси для API запросов
3. ✅ Очищен кэш Vite
4. ✅ Админка перезапущена

## 📋 Проверка:

1. Убедитесь, что админка запущена:
   ```bash
   lsof -ti:5174
   ```
   Должен показать PID процесса

2. Откройте в браузере:
   ```
   http://localhost:5174
   ```

3. Вы должны увидеть:
   - Боковое меню с пунктами: Дашборд, Товары, Баннеры, Рассылки, Брошенные корзины
   - Дашборд с аналитикой продаж

## 🚀 Если админка не открывается:

1. Остановите все процессы:
   ```bash
   pkill -f "vite.*admin"
   ```

2. Очистите кэш:
   ```bash
   cd apps/admin
   rm -rf node_modules/.vite
   ```

3. Запустите заново:
   ```bash
   cd apps/admin
   pnpm dev
   ```

4. Откройте в браузере:
   ```
   http://localhost:5174
   ```

## 📝 Порты:

- **5173** - Магазин (Web)
- **5174** - Админка (Admin) ← **ИСПОЛЬЗУЙТЕ ЭТОТ**
- **3000** - API сервер

