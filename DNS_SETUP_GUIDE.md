# 🔧 Исправление DNS для optmramor.ru

## Проблема
Домен `optmramor.ru` указывает на неправильный IP адрес:
- **Текущий IP:** `188.225.35.51`
- **Нужный IP:** `94.241.141.194`

## Решение: Настройка DNS записей

### Шаг 1: Найдите вашего DNS провайдера
Обычно это:
- Регистратор домена (где вы покупали домен)
- Или отдельный DNS-сервис (Cloudflare, Яндекс.Коннект, и т.д.)

### Шаг 2: Настройте A-запись

В панели управления DNS создайте/измените следующие записи:

#### Основная запись (корневой домен):
```
Тип: A
Имя: @ (или optmramor.ru, или оставьте пустым)
Значение: 94.241.141.194
TTL: 3600 (или минимальный доступный)
```

#### Запись для www:
```
Тип: A
Имя: www
Значение: 94.241.141.194
TTL: 3600
```

### Шаг 3: Проверка после настройки

После изменения DNS подождите **5-30 минут** (в зависимости от TTL) и проверьте:

```bash
# Проверка DNS
nslookup optmramor.ru
# Должно показать: 94.241.141.194

# Или
dig optmramor.ru +short
# Должно вернуть: 94.241.141.194
```

### Шаг 4: Проверка доступности сайта

```bash
# По IP (должно работать сразу)
curl -I http://94.241.141.194

# По домену (после обновления DNS)
curl -I http://optmramor.ru
```

## Инструкции для популярных DNS-провайдеров

### Яндекс.Коннект
1. Зайдите в [dns.yandex.ru](https://dns.yandex.ru)
2. Выберите домен `optmramor.ru`
3. Добавьте/измените A-запись:
   - Тип: A
   - Поддомен: @
   - IP: 94.241.141.194
4. Сохраните

### Cloudflare
1. Зайдите в [dash.cloudflare.com](https://dash.cloudflare.com)
2. Выберите домен
3. Перейдите в DNS → Records
4. Добавьте/измените:
   - Type: A
   - Name: @
   - IPv4 address: 94.241.141.194
   - Proxy: Off (для начала)
5. Сохраните

### REG.RU / Timeweb / другие регистраторы
1. Зайдите в панель управления доменом
2. Найдите раздел "DNS" или "Управление DNS"
3. Добавьте/измените A-запись:
   - Имя: @ или optmramor.ru
   - Тип: A
   - Значение: 94.241.141.194
4. Сохраните изменения

## ⚠️ Важно

1. **Время обновления DNS:** Обычно 5-30 минут, но может занять до 48 часов
2. **Кэш DNS:** Ваш браузер может кэшировать старый IP. Попробуйте:
   - Очистить кэш DNS: `sudo dscacheutil -flushcache` (macOS)
   - Использовать приватный режим браузера
   - Использовать другой DNS сервер (например, 8.8.8.8)
3. **Проверка на разных серверах:**
   ```bash
   # Google DNS
   nslookup optmramor.ru 8.8.8.8
   
   # Cloudflare DNS
   nslookup optmramor.ru 1.1.1.1
   ```

## Текущий статус

✅ **Сервер работает** - сайт доступен по IP `94.241.141.194`  
❌ **DNS не настроен** - домен указывает на старый IP `188.225.35.51`

После настройки DNS сайт будет доступен по адресу `http://optmramor.ru`



