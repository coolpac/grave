# 🔑 Получение Dev токена для админ-панели

## Способ 1: Через страницу логина (Рекомендуется)

1. Откройте админ-панель: `http://localhost:5174/login`
2. В режиме разработки вы увидите кнопку **"Получить dev токен"**
3. Нажмите на неё - токен автоматически сохранится и вы будете перенаправлены в админку

## Способ 2: Через API эндпоинт

### Через браузер:
```
GET http://localhost:3000/api/auth/dev-token
```

Или с указанием telegramId:
```
GET http://localhost:3000/api/auth/dev-token?telegramId=YOUR_TELEGRAM_ID
```

### Через curl:
```bash
curl http://localhost:3000/api/auth/dev-token
```

### Через fetch в консоли браузера:
```javascript
fetch('http://localhost:3000/api/auth/dev-token')
  .then(res => res.json())
  .then(data => {
    console.log('Token:', data.token);
    localStorage.setItem('authToken', data.token);
    window.location.href = '/';
  });
```

## Способ 3: Вручную через localStorage

1. Получите токен любым из способов выше
2. Откройте консоль браузера (F12)
3. Выполните:
```javascript
localStorage.setItem('authToken', 'ВАШ_ТОКЕН_ЗДЕСЬ');
window.location.href = '/';
```

## Важно

- ⚠️ Dev токен работает **только в режиме разработки** (NODE_ENV !== 'production')
- 🔒 В продакшене используйте обычную авторизацию через Telegram initData
- ⏰ Dev токен действителен **30 дней**
- 👤 Если пользователь с указанным telegramId не существует, он будет создан автоматически с ролью ADMIN

## Проверка токена

После получения токена проверьте, что он работает:
```javascript
// В консоли браузера
const token = localStorage.getItem('authToken');
console.log('Token:', token ? 'Есть' : 'Нет');
```

## Устранение проблем

Если токен не работает:
1. Убедитесь, что API сервер запущен на `http://localhost:3000`
2. Проверьте, что вы в режиме разработки (NODE_ENV !== 'production')
3. Очистите localStorage и получите новый токен
4. Проверьте консоль браузера на наличие ошибок

