# Быстрая настройка облачной базы данных

## Рекомендуется: Supabase (бесплатно, быстро)

### Шаг 1: Создайте проект на Supabase

1. Перейдите на [https://supabase.com](https://supabase.com)
2. Нажмите "Start your project"
3. Войдите через GitHub (быстрее всего)
4. Нажмите "New Project"
5. Заполните:
   - **Name**: ritual-shop (или любое имя)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший
6. Нажмите "Create new project"
7. Подождите 2-3 минуты пока проект создается

### Шаг 2: Получите DATABASE_URL

1. После создания проекта перейдите в **Settings** (шестеренка слева)
2. Выберите **Database**
3. Прокрутите до секции **Connection string**
4. Выберите вкладку **URI**
5. Скопируйте строку подключения (она выглядит так):
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

### Шаг 3: Обновите .env файл

Откройте `apps/api/.env` и замените строку:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/ritual_shop?schema=public"
```

На скопированную строку из Supabase (замените `[YOUR-PASSWORD]` на ваш пароль).

### Шаг 4: Выполните миграции

```bash
cd apps/api
npx prisma migrate dev --name add_admin_features
npx prisma generate
```

Готово! 🎉

---

## Альтернатива: Neon (тоже бесплатно)

### Шаг 1: Создайте проект на Neon

1. Перейдите на [https://neon.tech](https://neon.tech)
2. Нажмите "Sign Up" (можно через GitHub)
3. Нажмите "Create a project"
4. Заполните:
   - **Project name**: ritual-shop
   - **Region**: выберите ближайший
5. Нажмите "Create project"

### Шаг 2: Получите DATABASE_URL

1. После создания проекта вы увидите Connection String
2. Скопируйте его (кнопка "Copy")

### Шаг 3: Обновите .env файл

Вставьте скопированный Connection String в `DATABASE_URL` в файле `.env`

### Шаг 4: Выполните миграции

```bash
cd apps/api
npx prisma migrate dev --name add_admin_features
npx prisma generate
```

---

## Проверка подключения

После настройки проверьте:

```bash
cd apps/api
npx prisma db pull
```

Если команда выполнилась без ошибок - все работает!

## Просмотр данных

```bash
npx prisma studio
```

Откроется веб-интерфейс для просмотра и редактирования данных.

