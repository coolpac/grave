# 🔴 Настройка Redis для продакшена

## Быстрая установка

### macOS
```bash
brew install redis
brew services start redis
```

### Linux (Ubuntu/Debian)
```bash
sudo apt-get update
sudo apt-get install redis-server
sudo systemctl start redis-server
sudo systemctl enable redis-server
```

### Docker
```bash
docker run -d --name redis -p 6379:6379 redis:7-alpine
```

## Настройка переменных окружения

Добавьте в `apps/api/.env`:

```env
# Для продакшена - используйте Redis
NODE_ENV=production
REDIS_URL=redis://localhost:6379

# Или отдельно:
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_password_here

# Настройки кэша
CACHE_TTL=300
CACHE_MAX_ITEMS=1000
```

## Проверка работы

```bash
# Проверка Redis
redis-cli ping
# Должно вернуть: PONG

# Проверка из приложения
curl http://localhost:3000/api/products
# Первый запрос - из БД (медленнее)
# Второй запрос - из кэша (быстрее)
```

## Облачные решения

### Redis Cloud (бесплатный план)
1. Зарегистрируйтесь на https://redis.com/try-free/
2. Создайте базу данных
3. Скопируйте `REDIS_URL` из настроек
4. Добавьте в `.env`:
   ```env
   REDIS_URL=redis://default:password@host:port
   ```

### AWS ElastiCache
```env
REDIS_HOST=your-elasticache-endpoint.cache.amazonaws.com
REDIS_PORT=6379
```

### DigitalOcean Managed Redis
```env
REDIS_URL=rediss://default:password@host:port
```

## Автоматическое переключение

Система автоматически:
- ✅ Использует **Redis** в продакшене (`NODE_ENV=production`) при наличии `REDIS_URL` или `REDIS_HOST`
- ✅ Использует **in-memory кэш** в разработке или если Redis недоступен
- ✅ Логирует предупреждения при недоступности Redis в продакшене

## Отладка

```bash
# Подключение к Redis CLI
redis-cli

# Просмотр всех ключей
KEYS *

# Просмотр конкретного ключа
GET "categories"

# Очистка кэша
FLUSHALL

# Статистика
INFO stats
```

## Производительность

### Ожидаемые результаты:
- **In-memory кэш**: ~10-50ms
- **Redis (локальный)**: ~5-20ms
- **Redis (удаленный)**: ~20-100ms (зависит от сети)

### Рекомендации:
- Используйте локальный Redis для максимальной производительности
- Для распределенных систем используйте managed Redis
- Настройте TTL в зависимости от частоты обновления данных

