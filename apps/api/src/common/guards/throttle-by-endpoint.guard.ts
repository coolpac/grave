/**
 * Note: This custom guard is not needed as @Throttle decorator
 * is already supported by @nestjs/throttler package.
 * 
 * The @Throttle decorator on controllers automatically applies
 * the specified rate limits.
 * 
 * See auth.controller.ts and orders.controller.ts for examples.
 */

