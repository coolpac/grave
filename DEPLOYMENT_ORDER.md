# 📋 Порядок деплоя - можно ли деплоить без полного .env?

## ✅ Ответ: ДА, можно!

Вы можете задеплоить приложение с минимальным набором переменных, а потом добавить остальные.

---

## 🚀 Минимальный набор для первого деплоя

**Обязательно заполните только эти поля:**

```env
# 1. База данных
POSTGRES_PASSWORD=postgres  # или любой другой пароль
REDIS_PASSWORD=redis        # или любой другой пароль

# 2. JWT Secret (минимум 32 символа!)
JWT_SECRET=ВАШ_СЕКРЕТ_32_СИМВОЛА

# 3. Telegram основной бот
BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER

# 4. Домен
FRONTEND_URL=https://optmramor.ru
PUBLIC_URL=https://optmramor.ru
```

**Остальные поля можно оставить пустыми или заполнить позже!**

---

## 📝 Что можно добавить ПОСЛЕ деплоя

Эти поля **опциональны** и не блокируют запуск:

```env
# Токены для отдельных ботов (если нужны)
CUSTOMER_BOT_TOKEN=
ADMIN_BOT_TOKEN=

# Уведомления (можно настроить позже)
TELEGRAM_MANAGER_CHAT_ID=
ADMIN_WHITELIST=

# Мониторинг (опционально)
SENTRY_DSN=
GRAFANA_ADMIN_PASSWORD=
```

---

## 🔧 Про user bot (CUSTOMER_BOT_TOKEN)

### Важно понимать:

1. **CUSTOMER_BOT_TOKEN** используется в **Python боте** (`bots/customer_bot.py`), а не в NestJS коде напрямую
2. В NestJS коде используется **CUSTOMER_BOT_API_URL** - это URL к Python боту
3. **В коде менять ничего не нужно!** Код уже поддерживает опциональные токены

### Как это работает:

- Если `CUSTOMER_BOT_TOKEN` не указан → Python бот не будет работать, но API запустится
- Если `CUSTOMER_BOT_API_URL` не указан → API будет пытаться подключиться к `http://localhost:8001`
- Если бот недоступен → API просто логирует предупреждение, но продолжает работать

**Вывод:** Код уже готов, менять ничего не нужно! Просто добавьте токены в .env когда будете готовы.

---

## 📦 Порядок действий

### Шаг 1: Минимальный деплой

```bash
# 1. Создайте .env.production с минимумом
cp env.production.template .env.production
vim .env.production  # Заполните только обязательные поля

# 2. Задеплойте
./deploy.sh production
```

### Шаг 2: Проверьте что всё работает

```bash
# На сервере
ssh root@94.241.141.194
cd /opt/ritual-app
docker-compose -f docker-compose.production.yml ps
curl http://localhost:3000/api/health
```

### Шаг 3: Добавьте остальные настройки (когда будете готовы)

```bash
# На сервере отредактируйте .env
vim /opt/ritual-app/.env

# Добавьте недостающие поля:
# CUSTOMER_BOT_TOKEN=...
# ADMIN_BOT_TOKEN=...
# TELEGRAM_MANAGER_CHAT_ID=...

# Перезапустите API
docker-compose -f docker-compose.production.yml restart api
```

---

## ⚠️ Что произойдет если не заполнить опциональные поля?

### CUSTOMER_BOT_TOKEN / ADMIN_BOT_TOKEN
- ✅ API запустится и будет работать
- ⚠️ Уведомления через Python ботов не будут работать
- ✅ Основной функционал (заказы, товары, корзина) работает

### TELEGRAM_MANAGER_CHAT_ID
- ✅ API запустится
- ⚠️ Уведомления администратору не будут отправляться

### ADMIN_WHITELIST
- ✅ API запустится
- ⚠️ Админ-панель будет доступна всем (если не настроена другая защита)

### SENTRY_DSN
- ✅ API запустится
- ⚠️ Ошибки не будут отправляться в Sentry

---

## ✅ Итог

1. **Код менять не нужно** - всё уже поддерживает опциональные токены
2. **Можно деплоить с минимумом** - только обязательные поля
3. **Остальное можно добавить позже** - просто отредактируйте .env и перезапустите

**Минимальный набор для запуска:**
- POSTGRES_PASSWORD
- REDIS_PASSWORD  
- JWT_SECRET (32+ символа)
- BOT_TOKEN
- FRONTEND_URL
- PUBLIC_URL

Всё остальное - опционально! 🎉

