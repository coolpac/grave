tunnel: your-tunnel-id
credentials-file: /Users/who/гробы/.cloudflared/your-tunnel-id.json

ingress:
  # API сервер
  - hostname: your-api-subdomain.trycloudflare.com
    service: http://localhost:3000
  # Web фронтенд (если нужно)
  - hostname: your-web-subdomain.trycloudflare.com
    service: http://localhost:5173
  # Catch-all правило
  - service: http_status:404



