# 🔧 Исправление SSH и переменных окружения

## Проблема 1: SSH требует пароль

Хотя ключ был скопирован, SSH все еще требует пароль. Это происходит потому, что:
1. Ключ может быть не правильно добавлен в `authorized_keys` на сервере
2. Права доступа могут быть неправильными
3. ssh-agent не настроен для автоматического использования ключа

## Решение SSH

### Вариант 1: Автоматическая настройка (локально)

```bash
./fix-ssh-complete.sh
```

Этот скрипт:
- Покажет ваш публичный ключ
- Попытается скопировать его на сервер
- Настроит ssh-agent для автоматического использования ключа

### Вариант 2: Ручная настройка на сервере

**На сервере (где вы уже подключены):**

```bash
# 1. Создайте директорию .ssh если нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# 2. Получите публичный ключ с локальной машины
# На локальной машине выполните:
cat ~/.ssh/id_ed25519.pub

# 3. На сервере добавьте ключ (замените YOUR_KEY на ключ из шага 2)
echo "YOUR_KEY" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# 4. Проверьте содержимое
cat ~/.ssh/authorized_keys

# 5. Проверьте права
ls -la ~/.ssh/
```

**На локальной машине:**

```bash
# Настройте ssh-agent для автоматического использования ключа
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Теперь попробуйте подключиться
ssh root@94.241.141.194
```

---

## Проблема 2: Отсутствуют переменные окружения

Из логов видно:
```
ERROR [ExceptionHandler] Config validation error: "CUSTOMER_BOT_TOKEN" is not allowed to be empty
ERROR [ExceptionHandler] Config validation error: "ADMIN_BOT_TOKEN" is not allowed to be empty
```

API не может запуститься, потому что не установлены токены ботов.

## Решение переменных окружения

### На сервере проверьте .env файл:

```bash
cd /opt/ritual-app
cat .env | grep -E "CUSTOMER_BOT_TOKEN|ADMIN_BOT_TOKEN"
```

### Если переменные отсутствуют, добавьте их:

```bash
cd /opt/ritual-app

# Отредактируйте .env файл
nano .env

# Добавьте или обновите:
CUSTOMER_BOT_TOKEN=your_customer_bot_token_here
ADMIN_BOT_TOKEN=your_admin_bot_token_here
```

### После добавления переменных перезапустите API:

```bash
cd /opt/ritual-app
docker-compose -f docker-compose.production.yml restart api

# Проверьте логи
docker-compose -f docker-compose.production.yml logs -f api
```

---

## Быстрая проверка после исправлений

```bash
# 1. Проверка SSH (должно работать без пароля)
ssh root@94.241.141.194 "echo 'SSH работает!'"

# 2. Проверка переменных окружения
ssh root@94.241.141.194 "cd /opt/ritual-app && grep -E 'CUSTOMER_BOT_TOKEN|ADMIN_BOT_TOKEN' .env"

# 3. Проверка статуса контейнеров
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml ps"

# 4. Проверка логов API
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml logs --tail=20 api"
```

---

## Если SSH все еще требует пароль

1. **Проверьте на сервере права доступа:**
   ```bash
   ls -la ~/.ssh/
   # Должно быть:
   # drwx------  .ssh
   # -rw-------  authorized_keys
   ```

2. **Проверьте содержимое authorized_keys:**
   ```bash
   cat ~/.ssh/authorized_keys
   # Должен содержать ваш публичный ключ
   ```

3. **Проверьте логи SSH на сервере:**
   ```bash
   tail -f /var/log/auth.log
   # Или
   journalctl -u ssh -f
   ```

4. **Проверьте настройки SSH сервера:**
   ```bash
   grep -E "PubkeyAuthentication|PasswordAuthentication" /etc/ssh/sshd_config
   ```

---

## Примечание

Если у вашего SSH ключа есть passphrase, вам все равно нужно будет вводить его один раз при подключении, если ssh-agent не настроен. Настройка ssh-agent решает эту проблему.

