# 🛒 Интеграция брошенных корзин с localStorage

## ✅ Реализовано

### 1. Автоматическое отслеживание брошенных корзин

**Файл:** `apps/api/src/cart/cart-abandoned.service.ts`

**Возможности:**
- ✅ Автоматическая проверка корзин каждые 24 часа (cron job)
- ✅ Создание записей о брошенных корзинах
- ✅ Отметка корзин как восстановленных при оформлении заказа
- ✅ Проверка активности корзины при обновлении

### 2. Интеграция с localStorage

**Как это работает:**

1. **Пользователь добавляет товары в корзину:**
   - Данные сохраняются в localStorage через хук `useCart`
   - Одновременно синхронизируются с сервером (если авторизован)

2. **Корзина становится неактивной:**
   - Если корзина не обновлялась более 24 часов
   - Cron job автоматически создает запись `AbandonedCart`
   - Данные из localStorage остаются доступными

3. **Пользователь возвращается:**
   - При загрузке страницы корзина восстанавливается из localStorage
   - При авторизации локальные данные синхронизируются с сервером
   - Если корзина была помечена как брошенная, она автоматически восстанавливается

4. **Оформление заказа:**
   - При создании заказа корзина отмечается как восстановленная
   - localStorage очищается после успешного оформления

### 3. Улучшенная админка

**Файл:** `apps/admin/src/pages/AbandonedCarts.tsx`

**Возможности:**
- ✅ Список всех брошенных корзин
- ✅ Статистика (всего, общая стоимость, восстановлено)
- ✅ Отправка напоминаний через Telegram
- ✅ Информация о клиенте и товарах
- ✅ Отслеживание количества напоминаний
- ✅ Фильтрация по статусу (брошенные/восстановленные)

### 4. Уведомления через Telegram

**Интеграция с Python ботами:**
- Отправка напоминаний клиентам о брошенных корзинах
- Информация о товарах и сумме
- Количество дней с момента брошения

## 🔧 Технические детали

### Cron Job

```typescript
@Cron(CronExpression.EVERY_HOUR)
async checkAndCreateAbandonedCarts() {
  // Проверяет корзины, не обновлявшиеся более 24 часов
  // Создает записи AbandonedCart
}
```

### Порог неактивности

По умолчанию: **24 часа**

Можно настроить через переменную окружения:
```env
ABANDONMENT_THRESHOLD_HOURS=24
```

### Автоматическое восстановление

Корзина автоматически отмечается как восстановленная при:
- Оформлении заказа из этой корзины
- Обновлении корзины (добавление/изменение товаров)

## 📊 Структура данных

### AbandonedCart (БД)

```typescript
{
  id: number;
  userId: number;
  cartId: number;
  itemsCount: number;
  totalAmount: number;
  reminderSent: number;
  lastReminderAt: Date | null;
  recovered: boolean;
  recoveredAt: Date | null;
  createdAt: Date;
  updatedAt: Date;
}
```

### localStorage (Фронтенд)

```typescript
{
  cart_items: [
    {
      productId: number;
      variantId?: number;
      quantity: number;
      productSlug: string;
      productName: string;
      productPrice?: number;
      variantPrice?: number;
      variantName?: string;
      imageUrl?: string;
      timestamp: number;
    }
  ]
}
```

## 🚀 Использование

### Для администратора

1. **Просмотр брошенных корзин:**
   - Откройте админку → Брошенные корзины
   - Увидите список всех неоформленных корзин

2. **Отправка напоминаний:**
   - Нажмите "Отправить напоминание" на нужной корзине
   - Клиент получит уведомление в Telegram (если есть telegramId)

3. **Статистика:**
   - Общее количество брошенных корзин
   - Потенциальная выручка
   - Количество восстановленных корзин

### Для пользователя

1. **Добавление товаров:**
   - Товары автоматически сохраняются в localStorage
   - Синхронизируются с сервером при авторизации

2. **Возврат к корзине:**
   - При следующем посещении корзина восстанавливается из localStorage
   - Можно продолжить оформление заказа

3. **Уведомления:**
   - При брошении корзины (через 24 часа) может прийти напоминание в Telegram

## 🔄 Процесс работы

```
1. Пользователь добавляет товары → localStorage + сервер
2. Проходит 24 часа без активности → создается AbandonedCart
3. Cron job проверяет корзины каждый час
4. Администратор видит брошенные корзины в админке
5. Администратор отправляет напоминание → Telegram уведомление
6. Пользователь возвращается → корзина восстанавливается из localStorage
7. Пользователь оформляет заказ → корзина отмечается как восстановленная
```

## 📈 Преимущества

1. **Не теряются данные** - корзина сохраняется в localStorage
2. **Автоматизация** - не нужно вручную отслеживать брошенные корзины
3. **Восстановление** - пользователи могут вернуться и продолжить покупку
4. **Уведомления** - автоматические напоминания через Telegram
5. **Аналитика** - статистика по брошенным корзинам в админке

## ⚙️ Настройка

### Переменные окружения

```env
# Порог неактивности (часы)
ABANDONMENT_THRESHOLD_HOURS=24

# URLs Python ботов для уведомлений
CUSTOMER_BOT_API_URL=http://localhost:8001
ADMIN_BOT_API_URL=http://localhost:8002
```

### Cron Schedule

По умолчанию проверка выполняется каждый час. Можно изменить в `cart-abandoned.service.ts`:

```typescript
@Cron(CronExpression.EVERY_HOUR) // Каждый час
// или
@Cron('0 */6 * * *') // Каждые 6 часов
```

## 🧪 Тестирование

### Проверка автоматического создания

1. Добавьте товары в корзину
2. Подождите 24 часа (или измените порог в коде для теста)
3. Проверьте админку → Брошенные корзины
4. Должна появиться запись о брошенной корзине

### Проверка восстановления

1. Создайте брошенную корзину
2. Оформите заказ из этой корзины
3. Проверьте, что корзина отмечена как восстановленная

### Проверка уведомлений

1. Создайте брошенную корзину для пользователя с telegramId
2. Нажмите "Отправить напоминание" в админке
3. Проверьте, что пользователь получил уведомление в Telegram

## 📝 API Endpoints

### GET /admin/abandoned-carts
Получить список брошенных корзин

**Ответ:**
```json
{
  "carts": [
    {
      "id": 1,
      "cartId": 123,
      "userId": 456,
      "customerName": "Иван Иванов",
      "telegramId": "123456789",
      "itemsCount": 3,
      "totalAmount": 15000,
      "reminderSent": 1,
      "lastReminderAt": "2024-01-01T12:00:00Z",
      "createdAt": "2024-01-01T00:00:00Z",
      "daysSinceAbandoned": 1,
      "recovered": false,
      "recoveredAt": null
    }
  ],
  "stats": {
    "totalCount": 10,
    "totalValue": 150000,
    "averageValue": 15000,
    "recoveredCount": 5
  }
}
```

### POST /admin/abandoned-carts/:id/send-reminder
Отправить напоминание о брошенной корзине

**Ответ:**
```json
{
  "id": 1,
  "reminderSent": 2,
  "lastReminderAt": "2024-01-01T12:00:00Z"
}
```

## ✅ Готово к использованию

Система полностью интегрирована и готова к работе:
- ✅ Автоматическое отслеживание
- ✅ Интеграция с localStorage
- ✅ Админка для управления
- ✅ Уведомления через Telegram
- ✅ Статистика и аналитика

