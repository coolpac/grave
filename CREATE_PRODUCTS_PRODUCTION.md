# 🚀 Создание товаров на продакшене

Это руководство поможет вам создать товары на продакшен-сервере, используя тот же подход, что и на локальной машине.

## 📋 Подготовка

### 1. Убедитесь, что категории созданы

Перед созданием товаров убедитесь, что все необходимые категории уже созданы на продакшене. Если нет - создайте их через админ-панель или используйте скрипт создания категорий.

### 2. Получите токен авторизации

Для работы скрипта нужен токен администратора. Есть несколько способов получить его:

#### Способ 1: Через админ-панель (Рекомендуется)

1. Откройте админ-панель в браузере: `https://your-domain.com/admin`
2. Войдите через Telegram (используйте Telegram WebApp)
3. Откройте консоль браузера (F12 → Console)
4. Выполните команду:
   ```javascript
   localStorage.getItem('authToken')
   ```
5. Скопируйте полученный токен

#### Способ 2: Через API (если есть доступ)

Если у вас есть доступ к серверу и вы можете выполнить запрос:
```bash
curl -X POST https://your-domain.com/api/auth/validate \
  -H "Content-Type: application/json" \
  -d '{"initData": "YOUR_TELEGRAM_INIT_DATA"}'
```

## 🎯 Запуск скрипта

### Вариант 1: Интерактивный режим (Рекомендуется)

Скрипт запросит все необходимые данные:

```bash
cd apps/api
npx tsx scripts/create-products-production.ts --interactive
```

Или короткая версия:
```bash
npx tsx scripts/create-products-production.ts -i
```

### Вариант 2: Через переменные окружения

```bash
cd apps/api

# Установите переменные окружения
export API_URL=https://your-domain.com/api
export AUTH_TOKEN=your_token_here

# Запустите скрипт
npx tsx scripts/create-products-production.ts
```

### Вариант 3: Одной командой

```bash
cd apps/api
API_URL=https://your-domain.com/api AUTH_TOKEN=your_token npx tsx scripts/create-products-production.ts
```

## 🔒 Безопасность

Скрипт включает следующие меры безопасности:

- ✅ **Проверка подключения** - перед созданием товаров проверяется доступность API
- ✅ **Подтверждение действия** - в интерактивном режиме требуется подтверждение
- ✅ **Валидация токена** - токен проверяется при первом запросе
- ✅ **Обработка ошибок** - все ошибки логируются с деталями

## 📦 Что создается

Скрипт создаст следующие товары:

### Каталог 1 (Плита из мрамора, Крошка) - 5 товаров:

1. **Плита мраморная полированная** (MATRIX: Размер × Сорт)
   - Категория: `marble-slabs`
   - 4 варианта

2. **Заказная плита** (SINGLE_VARIANT: Толщина)
   - Категория: `marble-slabs`
   - 6 вариантов

3. **Плитка тротуарная** (SIMPLE)
   - Категория: `marble-slabs`
   - Цена по запросу

4. **Мраморная брекчия** (SINGLE_VARIANT: Форма)
   - Категория: `marble-slabs`
   - 2 варианта

5. **Декоративная мраморная крошка, песок** (MATRIX: Фракция × Упаковка)
   - Категория: `marble-chips`
   - 6 вариантов

### Каталог 3 (Ритуальные изделия) - 4 товара:

1. **Стела ритуальная** (MATRIX: Высота × Размер сечения)
   - Категория: `ritual-steles`
   - 15 вариантов

2. **Тумба ритуальная** (MATRIX: Размер основания × Высота)
   - Категория: `ritual-pedestals`
   - 28 вариантов

3. **Цветник ритуальный** (MATRIX: Размер × Обработка)
   - Категория: `ritual-flowerbeds`
   - 7 вариантов

4. **Ритуальная ваза** (MATRIX: Высота × Диаметр)
   - Категория: `marble-vases`
   - 14 вариантов

**Всего: 9 товаров с 83 вариантами**

## ✅ Проверка

После запуска скрипта:

1. Откройте админ-панель: `https://your-domain.com/admin/products`
2. Убедитесь, что все товары созданы
3. Проверьте, что варианты и атрибуты настроены корректно
4. Проверьте отображение товаров на фронтенде

## 🐛 Решение проблем

### Ошибка: "Категория с slug не найдена"

**Решение:** Убедитесь, что категории созданы. Запустите скрипт создания категорий или создайте их вручную через админ-панель.

### Ошибка: "Unauthorized" или "401"

**Решение:** 
- Проверьте, что токен актуален (не истек)
- Получите новый токен через админ-панель
- Убедитесь, что токен скопирован полностью (без пробелов)

### Ошибка: "Connection timeout"

**Решение:**
- Проверьте доступность API сервера
- Убедитесь, что URL правильный (должен заканчиваться на `/api`)
- Проверьте сетевые настройки и firewall

### Ошибка: "Product with slug already exists"

**Решение:** Товар с таким slug уже существует. Вы можете:
- Удалить существующий товар через админ-панель
- Изменить slug в скрипте
- Пропустить создание этого товара (закомментировать соответствующий код)

## 📝 Добавление новых товаров

Чтобы добавить товары из следующего прайс-листа:

1. Откройте файл `apps/api/scripts/create-products-production.ts`
2. Добавьте новые вызовы `createProduct()` в функцию `main()`
3. Убедитесь, что указаны правильные категории и материалы
4. Запустите скрипт снова

## 🔄 Обновление существующих товаров

Скрипт создает товары с помощью `POST /api/products`. Если товар с таким slug уже существует, будет ошибка.

Для обновления существующих товаров:
1. Используйте админ-панель
2. Или создайте отдельный скрипт для обновления (используя `PUT /api/products/:id`)

## 💡 Советы

- **Резервное копирование:** Перед массовым созданием товаров сделайте резервную копию базы данных
- **Тестирование:** Сначала создайте 1-2 товара для проверки, затем запустите полный скрипт
- **Логирование:** Все действия скрипта логируются в консоль, сохраните вывод для отладки
- **Постепенное добавление:** Если товаров много, можно разбить скрипт на части и запускать по очереди

## 📚 Дополнительные ресурсы

- [Руководство по созданию товаров локально](./CREATE_PRODUCTS_GUIDE.md)
- [Документация API](../apps/api/README.md)
- [Настройка продакшена](./PRODUCTION_SETUP.md)

