# 📝 Подробная инструкция по заполнению .env.production

## Как заполнить файл .env.production

### Шаг 1: Создайте файл

```bash
cp env.production.template .env.production
vim .env.production  # или nano, или любой редактор
```

---

## 🔐 Обязательные поля (ОБЯЗАТЕЛЬНО заполнить!)

### 1. База данных PostgreSQL

```env
POSTGRES_DB=ritual_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ВАШ_СИЛЬНЫЙ_ПАРОЛЬ_ЗДЕСЬ
```

**Как создать пароль:**
```bash
# Сгенерируйте случайный пароль (минимум 16 символов)
openssl rand -base64 24
```

**Пример:**
```env
POSTGRES_PASSWORD=K8mN2pQ9rT5vW8xZ1aB4cD7eF0gH3j
```

---

### 2. Redis

```env
REDIS_PASSWORD=ВАШ_СИЛЬНЫЙ_ПАРОЛЬ_ЗДЕСЬ
```

**Как создать пароль:**
```bash
openssl rand -base64 24
```

**Пример:**
```env
REDIS_PASSWORD=M3nP6qR9tV2wY5zA8bC1dE4fG7hJ0k
```

---

### 3. JWT Secret (для авторизации)

```env
JWT_SECRET=ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_МИНИМУМ_32_СИМВОЛА
```

**Как создать:**
```bash
openssl rand -base64 32
```

**Пример:**
```env
JWT_SECRET=K8mN2pQ9rT5vW8xZ1aB4cD7eF0gH3jK6mL9nP2qR5tV8wY1zA4bC7dE0fG3hJ6k
```

⚠️ **ВАЖНО:** Минимум 32 символа! Это критично для безопасности.

---

### 4. Telegram Bot Token (основной)

```env
BOT_TOKEN=ВАШ_ТОКЕН_ОТ_BOTFATHER
```

**Как получить:**
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Пример:**
```env
BOT_TOKEN=7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
```

---

## 📱 Опциональные поля Telegram (можно заполнить позже)

### Customer Bot Token (бот для клиентов)

Если у вас отдельный бот для отправки уведомлений клиентам:

```env
CUSTOMER_BOT_TOKEN=ТОКЕН_БОТА_ДЛЯ_КЛИЕНТОВ
```

**Как получить:**
1. Создайте еще одного бота через @BotFather
2. Скопируйте токен

**Если не указать:** Будет использоваться `BOT_TOKEN`

---

### Admin Bot Token (бот для администратора)

Если у вас отдельный бот для отправки уведомлений администратору:

```env
ADMIN_BOT_TOKEN=ТОКЕН_БОТА_ДЛЯ_АДМИНА
```

**Как получить:**
1. Создайте еще одного бота через @BotFather
2. Скопируйте токен

---

### Telegram Manager Chat ID

ID чата, куда отправлять уведомления администратору:

```env
TELEGRAM_MANAGER_CHAT_ID=ВАШ_TELEGRAM_ID
```

**Как узнать свой Telegram ID:**
1. Откройте Telegram
2. Найдите [@userinfobot](https://t.me/userinfobot)
3. Отправьте `/start`
4. Бот пришлет ваш ID (число, например: `123456789`)

**Пример:**
```env
TELEGRAM_MANAGER_CHAT_ID=123456789
```

---

### Admin Whitelist

Список Telegram ID администраторов через запятую:

```env
ADMIN_WHITELIST=123456789,987654321,555666777
```

**Как узнать ID:**
- Используйте [@userinfobot](https://t.me/userinfobot)

**Пример:**
```env
ADMIN_WHITELIST=123456789,987654321
```

---

## 🌐 URL настройки

### Frontend URL и Public URL

```env
FRONTEND_URL=https://optmramor.ru
PUBLIC_URL=https://optmramor.ru
```

**Важно:**
- Используйте `https://` если настроен SSL
- Используйте `http://` если SSL еще не настроен
- Домен: `optmramor.ru`

**Если SSL еще не настроен:**
```env
FRONTEND_URL=http://optmramor.ru
PUBLIC_URL=http://optmramor.ru
```

---

### Cloudflare Tunnel URL (опционально)

Если используете Cloudflare Tunnel, укажите URL:

```env
CLOUDFLARE_TUNNEL_URL=https://your-tunnel.trycloudflare.com
```

**Если не используете:** Оставьте пустым
```env
CLOUDFLARE_TUNNEL_URL=
```

---

## 🔧 Остальные настройки (можно оставить по умолчанию)

### Application Configuration

```env
NODE_ENV=production
API_PORT=3000
APP_VERSION=1.0.0
```

**Обычно не нужно менять.**

---

### JWT Expires In

```env
JWT_EXPIRES_IN=7d
```

**Варианты:**
- `7d` - 7 дней (рекомендуется)
- `30d` - 30 дней
- `24h` - 24 часа

---

### Logging

```env
LOG_LEVEL=info
LOG_TO_FILE=true
```

**LOG_LEVEL варианты:**
- `error` - только ошибки
- `warn` - предупреждения и ошибки
- `info` - информационные сообщения (рекомендуется)
- `debug` - все сообщения (для отладки)

---

### Sentry (опционально, для отслеживания ошибок)

Если используете Sentry для мониторинга ошибок:

```env
SENTRY_DSN=https://key@org.ingest.sentry.io/project
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1
```

**Если не используете:** Оставьте `SENTRY_DSN` пустым
```env
SENTRY_DSN=
```

---

### Monitoring (опционально)

```env
ENABLE_METRICS=true
PROMETHEUS_PORT=9090
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=ВАШ_ПАРОЛЬ_ДЛЯ_GRAFANA
GRAFANA_PORT=3001
```

**GRAFANA_ADMIN_PASSWORD:** Придумайте пароль для доступа к Grafana (если будете использовать мониторинг).

---

### Health Checks

```env
MEMORY_HEALTH_THRESHOLD=90
DISK_HEALTH_THRESHOLD=10
SHUTDOWN_TIMEOUT=10000
```

**Обычно не нужно менять.**

---

## ✅ Пример заполненного файла

```env
# ============================================
# DATABASE CONFIGURATION
# ============================================
POSTGRES_DB=ritual_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=K8mN2pQ9rT5vW8xZ1aB4cD7eF0gH3j
POSTGRES_PORT=5432

# ============================================
# REDIS CONFIGURATION
# ============================================
REDIS_PASSWORD=M3nP6qR9tV2wY5zA8bC1dE4fG7hJ0k
REDIS_PORT=6379

# ============================================
# APPLICATION CONFIGURATION
# ============================================
NODE_ENV=production
API_PORT=3000
APP_VERSION=1.0.0

# ============================================
# JWT CONFIGURATION
# ============================================
JWT_SECRET=K8mN2pQ9rT5vW8xZ1aB4cD7eF0gH3jK6mL9nP2qR5tV8wY1zA4bC7dE0fG3hJ6k
JWT_EXPIRES_IN=7d

# ============================================
# TELEGRAM CONFIGURATION
# ============================================
BOT_TOKEN=7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
CUSTOMER_BOT_TOKEN=7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
ADMIN_BOT_TOKEN=7987654321:BBIdqUcwDI2wHXKygTfpgTBs1L6QBMExbx
TELEGRAM_MANAGER_CHAT_ID=123456789
ADMIN_WHITELIST=123456789,987654321

# ============================================
# URLS
# ============================================
FRONTEND_URL=https://optmramor.ru
PUBLIC_URL=https://optmramor.ru
CLOUDFLARE_TUNNEL_URL=

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=info
LOG_TO_FILE=true

# ============================================
# SENTRY ERROR TRACKING (Optional)
# ============================================
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1

# ============================================
# MONITORING (Optional)
# ============================================
ENABLE_METRICS=true
PROMETHEUS_PORT=9090
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=MySecureGrafanaPassword123
GRAFANA_PORT=3001

# ============================================
# HEALTH CHECKS
# ============================================
MEMORY_HEALTH_THRESHOLD=90
DISK_HEALTH_THRESHOLD=10
SHUTDOWN_TIMEOUT=10000
```

---

## 🔍 Чеклист перед деплоем

Проверьте, что заполнены:

- [ ] `POSTGRES_PASSWORD` - сильный пароль (минимум 16 символов)
- [ ] `REDIS_PASSWORD` - сильный пароль (минимум 16 символов)
- [ ] `JWT_SECRET` - минимум 32 символа
- [ ] `BOT_TOKEN` - токен от @BotFather
- [ ] `FRONTEND_URL` - ваш домен (https://optmramor.ru)
- [ ] `PUBLIC_URL` - ваш домен (https://optmramor.ru)

Опционально (можно заполнить позже):

- [ ] `CUSTOMER_BOT_TOKEN` - если нужен отдельный бот для клиентов
- [ ] `ADMIN_BOT_TOKEN` - если нужен отдельный бот для админа
- [ ] `TELEGRAM_MANAGER_CHAT_ID` - ваш Telegram ID
- [ ] `ADMIN_WHITELIST` - список ID администраторов
- [ ] `GRAFANA_ADMIN_PASSWORD` - если будете использовать мониторинг

---

## ⚠️ Важные замечания

1. **НЕ коммитьте `.env.production` в Git!** Этот файл содержит секреты.

2. **Используйте сильные пароли** для БД и Redis.

3. **JWT_SECRET должен быть минимум 32 символа** - это критично для безопасности.

4. **Если SSL еще не настроен**, используйте `http://` вместо `https://` в URL.

5. **CUSTOMER_BOT_TOKEN и ADMIN_BOT_TOKEN опциональны** - если не указать, будет использоваться BOT_TOKEN.

---

## 🆘 Нужна помощь?

Если что-то непонятно:
1. Проверьте пример выше
2. См. `apps/api/ENV_VARIABLES.md` для подробного описания всех переменных
3. См. `DEPLOYMENT_QUICK_START.md` для инструкций по деплою

