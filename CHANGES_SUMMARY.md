# 📋 Сводка изменений

## ✅ Что было исправлено

### 1. Валидация initData теперь использует ADMIN_BOT_TOKEN

**Было:**
- Использовался только `BOT_TOKEN` для валидации Telegram initData

**Стало:**
- Сначала пробуется `ADMIN_BOT_TOKEN` (если указан)
- Затем пробуется `BOT_TOKEN` (как fallback)
- Это позволяет использовать админ-бота для валидации

**Файл:** `apps/api/src/auth/auth.service.ts`

---

### 2. Добавлен endpoint для получения токена администратора с телефона

**Новые endpoints:**
- `GET /api/auth/admin-token?telegramId=ВАШ_ID` - работает в production!
- `POST /api/auth/admin-token` с `initData` или `telegramId` в body

**Как использовать:**
```bash
# С телефона (самый простой способ):
curl "https://optmramor.ru/api/auth/admin-token?telegramId=83146995"

# Или через браузер:
https://optmramor.ru/api/auth/admin-token?telegramId=83146995
```

**Требования:**
- Telegram ID должен быть в `ADMIN_WHITELIST`

**Файлы:**
- `apps/api/src/auth/auth.service.ts` - метод `generateAdminToken()`
- `apps/api/src/auth/auth.controller.ts` - endpoints `GET/POST /admin-token`

---

### 3. Обновлена валидация переменных окружения

**Было:**
- `BOT_TOKEN` был обязателен в production

**Стало:**
- Либо `BOT_TOKEN`, либо `ADMIN_BOT_TOKEN` должен быть указан в production
- Оба могут быть опциональными (но хотя бы один нужен)

**Файл:** `apps/api/src/config/env.validation.ts`

---

## 🎯 Преимущества

1. **Можно использовать админ-бота для валидации** - больше гибкости
2. **Можно получить токен с телефона** - не нужно открывать админ-панель
3. **Проще настройка** - можно использовать только `ADMIN_BOT_TOKEN`

---

## 📝 Как использовать

### Настройка .env.production

**Вариант 1: Только ADMIN_BOT_TOKEN (рекомендуется)**
```env
ADMIN_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
BOT_TOKEN=
```

**Вариант 2: Только BOT_TOKEN (как раньше)**
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_BOT_TOKEN=
```

**Вариант 3: Оба токена**
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_BOT_TOKEN=987654321:XYZabcDEFghiJKLmnoPQRstuv
```

### Получение токена администратора

**С телефона:**
```bash
curl "https://optmramor.ru/api/auth/admin-token?telegramId=83146995"
```

**Через админ-панель (как раньше):**
1. Откройте `https://optmramor.ru/admin`
2. F12 → Console
3. `localStorage.getItem('authToken')`

---

## 🔄 Что нужно сделать после изменений

1. **Обновите .env.production:**
   - Убедитесь, что указан хотя бы один токен (`BOT_TOKEN` или `ADMIN_BOT_TOKEN`)
   - Если используете только `ADMIN_BOT_TOKEN`, можно оставить `BOT_TOKEN` пустым

2. **Перезапустите API:**
   ```bash
   ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml restart api"
   ```

3. **Проверьте работу:**
   ```bash
   # Проверьте получение токена
   curl "https://optmramor.ru/api/auth/admin-token?telegramId=ВАШ_ID"
   ```

---

## 📚 Документация

- `HOW_TO_GET_ADMIN_TOKEN_UPDATED.md` - подробная инструкция по получению токена
- `ENV_VARIABLES_USAGE.md` - использование переменных окружения

