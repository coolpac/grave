# 🔧 Настройка админ-панели

## ✅ Что настроено:

1. **Отдельный порт:** Админка запускается на порту `5174`
2. **API прокси:** Настроен прокси для `/api` → `http://localhost:3000`
3. **Отдельное приложение:** Админка полностью отделена от магазина

## 🚀 Запуск админки:

### Из корня проекта:
```bash
pnpm dev:admin
```

### Из директории админки:
```bash
cd apps/admin
pnpm dev
```

## 🌐 Доступ к админке:

После запуска админка будет доступна по адресу:
```
http://localhost:5174
```

## ⚠️ Важно:

1. **Магазин (Web)** работает на порту `5173`
2. **Админка (Admin)** работает на порту `5174`
3. **API** работает на порту `3000`

Убедитесь, что вы открываете правильный порт!

## 🔍 Проверка:

Если админка не открывается или показывает магазин:

1. Проверьте, что админка запущена:
   ```bash
   lsof -ti:5174
   ```

2. Убедитесь, что вы открываете правильный URL:
   ```
   http://localhost:5174
   ```
   НЕ `http://localhost:5173` (это магазин)

3. Перезапустите админку:
   ```bash
   # Остановите текущий процесс (Ctrl+C)
   cd apps/admin
   pnpm dev
   ```

4. Очистите кэш браузера или откройте в режиме инкогнито

## 📝 Структура админки:

- `/` - Дашборд с аналитикой
- `/products` - Управление товарами
- `/banners` - Управление баннерами
- `/newsletters` - Управление рассылками
- `/abandoned-carts` - Брошенные корзины

