# 🛒 Улучшение корзины с сохранением в localStorage

## ✅ Что было реализовано

### 1. Создан хук `useCart` для централизованного управления корзиной

**Файл:** `apps/web/src/hooks/useCart.ts`

**Возможности:**
- ✅ Автоматическое сохранение корзины в localStorage
- ✅ Загрузка корзины из localStorage при старте
- ✅ Синхронизация локальной и серверной корзины
- ✅ Поддержка офлайн режима
- ✅ Оптимистичные обновления (instant UI feedback)
- ✅ Автоматический retry при сетевых ошибках
- ✅ Debounce для сохранения в localStorage (500ms)
- ✅ Очистка устаревших данных (старше 30 дней)

### 2. Интеграция с React Query

- Использует `useQuery` для загрузки корзины с сервера
- Использует `useMutation` для оптимистичных обновлений
- Автоматическая инвалидация кэша при изменениях
- Retry логика при сетевых ошибках

### 3. Обновлены компоненты

- **Cart.tsx** - полностью переписан для использования хука `useCart`
- **Product.tsx** - обновлен для использования хука `useCart`
- **Category.tsx** - обновлен для использования хука `useCart`

## 🎯 Основные возможности

### Автоматическое сохранение

Корзина автоматически сохраняется в localStorage при каждом изменении:
- Добавление товара
- Изменение количества
- Удаление товара
- Очистка корзины

### Синхронизация с сервером

1. **При авторизации:**
   - Локальные элементы автоматически синхронизируются с сервером
   - Используется debounce (1 секунда) для избежания множественных запросов

2. **При изменении:**
   - Сначала обновляется UI (оптимистичное обновление)
   - Затем отправляется запрос на сервер
   - При ошибке происходит откат изменений

### Офлайн режим

- Работает без интернета
- Сохранение в localStorage
- Индикатор офлайн режима в UI
- Автоматическая синхронизация при восстановлении связи

### Объединение локальной и серверной корзины

При загрузке корзины:
1. Загружается корзина с сервера (если авторизован)
2. Загружается локальная корзина из localStorage
3. Объединяются: приоритет у серверных данных, но добавляются локальные элементы, которых нет на сервере

## 📋 Структура данных в localStorage

```typescript
interface LocalCartItem {
  productId: number;
  variantId?: number;
  quantity: number;
  productSlug: string;
  productName: string;
  productPrice?: number;
  variantPrice?: number;
  variantName?: string;
  imageUrl?: string;
  timestamp: number; // Для очистки устаревших данных
}
```

**Ключ:** `cart_items`

## 🔧 API хука useCart

```typescript
const {
  cart,           // Объект корзины { id, items }
  items,          // Массив элементов корзины
  total,          // Общая сумма
  itemsCount,      // Общее количество товаров
  isLoading,      // Загрузка корзины
  isOffline,      // Офлайн режим
  error,          // Ошибка загрузки
  updateQuantity, // (itemId, delta) => void
  removeItem,     // (itemId) => void
  addToCart,      // (productId, options?) => void
  clearCart,      // () => Promise<void>
  refetch,        // () => Promise<void>
} = useCart()
```

### Примеры использования

#### Добавление товара в корзину

```typescript
addToCart(productId, {
  variantId: 123,
  quantity: 1,
  productSlug: 'product-slug',
  productName: 'Название товара',
  productPrice: 5000,
  variantPrice: 5500,
  variantName: 'Вариант 1',
  imageUrl: 'https://example.com/image.jpg',
})
```

#### Изменение количества

```typescript
// Увеличить на 1
updateQuantity(itemId, 1)

// Уменьшить на 1
updateQuantity(itemId, -1)
```

#### Удаление товара

```typescript
removeItem(itemId)
```

## 🚀 Преимущества

1. **Сохранение между сессиями** - корзина не теряется при перезагрузке страницы
2. **Офлайн поддержка** - можно добавлять товары без интернета
3. **Мгновенная обратная связь** - оптимистичные обновления
4. **Надежность** - автоматическая синхронизация при восстановлении связи
5. **Производительность** - debounce для сохранения в localStorage
6. **Безопасность** - очистка устаревших данных

## 🔍 Обработка ошибок

- **Сетевые ошибки:** Сохранение в localStorage, синхронизация при восстановлении связи
- **Ошибки авторизации:** Работа в локальном режиме
- **Ошибки сервера:** Откат оптимистичных обновлений
- **Переполнение localStorage:** Очистка устаревших данных

## 📊 Производительность

- **Debounce сохранения:** 500ms
- **Debounce синхронизации:** 1000ms
- **TTL данных:** 30 дней
- **Автоматическая очистка:** При загрузке старше 30 дней

## 🧪 Тестирование

### Проверка сохранения в localStorage

1. Добавьте товар в корзину
2. Откройте DevTools → Application → Local Storage
3. Проверьте ключ `cart_items`
4. Перезагрузите страницу
5. Корзина должна восстановиться

### Проверка офлайн режима

1. Откройте DevTools → Network → Offline
2. Добавьте товар в корзину
3. Проверьте, что товар сохранился в localStorage
4. Включите сеть обратно
5. Проверьте синхронизацию с сервером

### Проверка синхронизации

1. Добавьте товар без авторизации (сохранится локально)
2. Авторизуйтесь
3. Проверьте, что товар синхронизировался с сервером
4. Проверьте, что локальная корзина очистилась

## 🔄 Миграция с старой системы

Старая система использовала:
- `sessionStorage` для временного хранения
- Прямые вызовы API без хука
- Локальное состояние в каждом компоненте

Новая система:
- `localStorage` для постоянного хранения
- Централизованный хук `useCart`
- Единое состояние через React Query

## 📝 Дополнительные улучшения (опционально)

В будущем можно добавить:
- [ ] Индикатор синхронизации
- [ ] Конфликт-резолюция при расхождении данных
- [ ] Сжатие данных в localStorage
- [ ] Экспорт/импорт корзины
- [ ] История изменений корзины

