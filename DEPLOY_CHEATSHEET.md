# 🚀 Шпаргалка по деплою

## Самый быстрый способ (РЕКОМЕНДУЕТСЯ)

```bash
./quick-deploy.sh
```

**Время:** 2-5 минут  
**Что делает:** Автоматически выбирает лучший метод (локальная сборка или сервер)

---

## Деплой только одного сервиса

```bash
./quick-deploy.sh api    # Только бэкенд (быстрее!)
./quick-deploy.sh web     # Только фронтенд (еще быстрее!)
```

---

## Ручной контроль (2 шага)

```bash
# 1. Собрать локально
./build-local.sh

# 2. Задеплоить
./deploy-local-images.sh
```

**Время:** 3-7 минут  
**Преимущество:** Не нагружает сервер

---

## Полный деплой на сервере (если нет Docker локально)

```bash
./deploy.sh production
```

**Время:** 10-20 минут  
**Когда использовать:** Нет Docker локально или нужна полная пересборка

---

## Полезные команды

```bash
# Статус контейнеров
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml ps"

# Логи
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml logs -f api"

# Перезапуск
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml restart api"
```

---

## ⚡ Быстрый workflow

```bash
# 1. Внесли изменения
git add . && git commit -m "feat: update"

# 2. Деплой
./quick-deploy.sh

# Готово! 🎉
```

---

**Подробнее:** См. `FAST_DEPLOY_GUIDE.md`



