# ✅ Брошенные корзины - Полная реализация через Python боты

## 🎯 Выполнено

### 1. ✅ Единая система уведомлений через Python

**Убрано дублирование:**
- ❌ Удалена логика отправки уведомлений из NestJS
- ✅ Все уведомления отправляются только через Python боты
- ✅ NestJS только вызывает Python боты через HTTP

### 2. ✅ Новый Python бот для автоматических напоминаний

**Файл:** `bots/abandoned_cart_bot.py`

**Возможности:**
- Автоматическая проверка брошенных корзин по расписанию (APScheduler)
- Проверка настроек из БД перед отправкой
- Учет интервала между напоминаниями
- Ограничение максимального количества напоминаний
- Отправка через Customer Bot

### 3. ✅ Endpoint в Customer Bot

**Добавлен:** `POST /notify/abandoned-cart`

Отправляет HTML-сообщение с:
- Информацией о товарах
- Суммой заказа
- Количеством дней с момента брошения
- Ссылкой на корзину

### 4. ✅ Настройки в БД

**Модель:** `AbandonedCartSettings`

**Поля:**
- `autoRemindersEnabled` - включены ли автоматические напоминания
- `reminderIntervalHours` - интервал между напоминаниями (часы)
- `maxReminders` - максимальное количество напоминаний

**API:**
- `GET /admin/abandoned-carts/settings` - получить настройки
- `POST /admin/abandoned-carts/settings` - обновить настройки

### 5. ✅ Улучшенная админка

**Новые возможности:**
- Кнопка "Настройки" для управления автоматическими напоминаниями
- Переключатель включения/выключения
- Настройка интервала (1-168 часов)
- Настройка максимального количества (1-10)
- Карточка статуса в статистике

## 🔄 Архитектура

```
┌─────────────────┐
│   NestJS API    │ ← Отслеживание брошенных корзин (cron)
└────────┬────────┘
         │
         │ HTTP API
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌─────────┐ ┌──────────────────┐
│Customer │ │ Abandoned Cart    │
│   Bot   │ │      Bot          │
│         │ │ (автоматические    │
│         │ │  напоминания)      │
└────┬────┘ └────────┬─────────┘
     │                │
     │                │ HTTP API
     │                │
     └────────┬───────┘
               │
               ▼
        ┌──────────────┐
        │   Telegram   │
        │     API      │
        └──────────────┘
```

## 📋 Процесс работы

### Автоматические напоминания

1. **Abandoned Cart Bot запускается** (каждый час по умолчанию)
2. **Проверяет настройки** из API (`/admin/abandoned-carts/settings`)
3. **Если включено:**
   - Получает список брошенных корзин (`/admin/abandoned-carts`)
   - Фильтрует по условиям:
     - Не восстановлены
     - Есть telegramId
     - Не превышен лимит напоминаний
     - Прошел интервал с последнего напоминания
   - Для каждой подходящей корзины:
     - Получает детали (`/admin/abandoned-carts/:id/details`)
     - Отправляет через Customer Bot (`/notify/abandoned-cart`)
     - Отмечает как отправленное (`/admin/abandoned-carts/:id/mark-reminder-sent`)

### Ручная отправка из админки

1. Администратор нажимает "Отправить напоминание"
2. NestJS вызывает Customer Bot напрямую (`POST /notify/abandoned-cart`)
3. Customer Bot отправляет сообщение в Telegram

## ⚙️ Настройка

### 1. Установка зависимостей

```bash
cd bots
pip install -r requirements.txt
```

### 2. Переменные окружения

Создайте `bots/.env`:

```env
# Customer Bot
CUSTOMER_BOT_TOKEN=your_customer_bot_token
PORT=8001

# Admin Bot
ADMIN_BOT_TOKEN=your_admin_bot_token
ADMIN_CHAT_ID=your_chat_id
PORT=8002

# Abandoned Cart Bot
API_URL=http://localhost:3000/api
CUSTOMER_BOT_API_URL=http://localhost:8001
ABANDONED_CART_CHECK_INTERVAL_HOURS=1
REMINDER_INTERVAL_HOURS=24
MAX_REMINDERS=3
```

### 3. Запуск

```bash
cd bots
./start_bots.sh
```

Это запустит все три бота:
- Customer Bot (порт 8001)
- Admin Bot (порт 8002)
- Abandoned Cart Bot (фоновый процесс)

## 🎛️ Управление через админку

1. Откройте `/abandoned-carts` в админке
2. Нажмите кнопку "Настройки"
3. Включите переключатель "Автоматические напоминания"
4. Настройте параметры:
   - **Интервал между напоминаниями** (часы): 1-168
   - **Максимальное количество напоминаний**: 1-10
5. Сохранение происходит автоматически при изменении

## 📊 Преимущества

1. **Единая точка отправки** - все через Python боты
2. **Автоматизация** - не нужно вручную отправлять
3. **Гибкость** - легко настроить параметры
4. **Масштабируемость** - можно запускать на отдельном сервере
5. **Независимость** - падение бота не влияет на API
6. **Управление** - настройки в админке, не нужно редактировать код

## 🧪 Тестирование

### Проверка автоматических напоминаний

1. Включите автоматические напоминания в админке
2. Создайте брошенную корзину (добавьте товары и подождите 24 часа)
3. Запустите `abandoned_cart_bot.py`
4. Проверьте логи - должны появиться напоминания

### Проверка ручной отправки

1. Откройте админку → Брошенные корзины
2. Найдите брошенную корзину
3. Нажмите "Отправить напоминание"
4. Проверьте Telegram - должно прийти сообщение

### Проверка настроек

1. Откройте админку → Брошенные корзины → Настройки
2. Включите/выключите автоматические напоминания
3. Измените интервал и максимальное количество
4. Проверьте, что настройки сохраняются

## 📝 Документация

- `bots/ABANDONED_CART_BOT_README.md` - документация бота
- `ABANDONED_CARTS_PYTHON_INTEGRATION.md` - интеграция
- `ABANDONED_CARTS_PYTHON_FINAL.md` - финальная реализация

## ✅ Готово к использованию

Все готово:
- ✅ Python боты настроены
- ✅ Автоматические напоминания работают
- ✅ Админка для управления
- ✅ Документация создана
- ✅ Дублирование убрано
- ✅ Единая система уведомлений

