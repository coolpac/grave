# 🚨 Руководство по восстановлению сервера

## Проблема: Сервер не отвечает или завис

### Быстрая диагностика

#### 1. Проверка доступности сервера

```bash
# Проверка ping
ping -c 3 94.241.141.194

# Если ping работает, но SSH нет - сервер перегружен
```

#### 2. Возможные причины

- 🔴 **Перегрузка памяти** - Docker сборка потребляет много RAM
- 🔴 **Перегрузка CPU** - компиляция TypeScript очень ресурсоемкая
- 🔴 **Заполнен диск** - Docker образы занимают много места
- 🔴 **SSH завис** - слишком много процессов

---

## ✅ Решения

### Решение 1: Подождать (если сборка идет)

Если Docker сборка все еще идет, просто подождите 5-10 минут. Сборка может занять время.

### Решение 2: Перезагрузка сервера через панель управления

Если у вас есть доступ к панели управления хостингом:
1. Зайдите в панель управления
2. Найдите опцию "Перезагрузка" или "Reboot"
3. Перезагрузите сервер
4. Подождите 2-3 минуты

### Решение 3: Принудительное подключение

```bash
# Попробуйте подключиться с таймаутом
ssh -o ConnectTimeout=10 -o ServerAliveInterval=5 root@94.241.141.194

# Если не получается, попробуйте через несколько минут
```

### Решение 4: Проверка через VNC/KVM (если доступно)

Если у вас есть доступ к VNC или KVM консоли:
1. Подключитесь к консоли
2. Проверьте состояние системы
3. Если система зависла - перезагрузите

---

## 🔧 После восстановления доступа

### 1. Проверьте состояние системы

```bash
ssh root@94.241.141.194

# Проверка памяти
free -h

# Проверка диска
df -h

# Проверка процессов
top
# или
htop

# Проверка Docker
docker ps
docker stats
```

### 2. Очистка ресурсов

```bash
# Остановите все контейнеры
docker-compose -f /opt/ritual-app/docker-compose.production.yml down

# Очистите неиспользуемые Docker ресурсы
docker system prune -a --volumes

# Проверьте место на диске
df -h
```

### 3. Проверьте логи

```bash
# Логи Docker
docker-compose -f /opt/ritual-app/docker-compose.production.yml logs --tail=100

# Системные логи
journalctl -xe --no-pager | tail -50
dmesg | tail -50
```

---

## 🛡️ Предотвращение проблем в будущем

### 1. Мониторинг ресурсов

Создайте скрипт для мониторинга:

```bash
# На сервере создайте /root/check-resources.sh
cat > /root/check-resources.sh << 'EOF'
#!/bin/bash
echo "=== Memory ==="
free -h
echo ""
echo "=== Disk ==="
df -h
echo ""
echo "=== Docker ==="
docker stats --no-stream
EOF

chmod +x /root/check-resources.sh
```

### 2. Ограничение ресурсов Docker

Добавьте в `docker-compose.production.yml`:

```yaml
services:
  api:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G
```

### 3. Сборка на локальной машине

Вместо сборки на сервере, собирайте локально и загружайте образы:

```bash
# На локальной машине
docker build -t ritual-api:latest -f apps/api/Dockerfile .
docker build -t ritual-web:latest -f apps/web/Dockerfile .

# Сохраните образы
docker save ritual-api:latest | gzip > ritual-api.tar.gz
docker save ritual-web:latest | gzip > ritual-web.tar.gz

# Загрузите на сервер
scp ritual-api.tar.gz ritual-web.tar.gz root@94.241.141.194:/tmp/

# На сервере загрузите
ssh root@94.241.141.194
docker load < /tmp/ritual-api.tar.gz
docker load < /tmp/ritual-web.tar.gz
```

---

## 📞 Контакты поддержки

Если ничего не помогает:
1. Обратитесь в поддержку хостинг-провайдера
2. Попросите перезагрузить сервер
3. Проверьте, не превышены ли лимиты ресурсов

---

## ⚡ Быстрые команды

```bash
# Проверка доступности
ping 94.241.141.194

# Подключение с таймаутом
ssh -o ConnectTimeout=10 root@94.241.141.194

# После подключения - проверка ресурсов
free -h && df -h && docker ps

# Очистка Docker
docker system prune -a --volumes

# Остановка всех контейнеров
docker stop $(docker ps -q)
```


