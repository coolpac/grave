# ⚡ Быстрый деплой - Руководство

## 🎯 Три способа деплоя (от быстрого к медленному)

### 1. 🚀 Быстрый деплой (РЕКОМЕНДУЕТСЯ) - ~2-5 минут

**Используйте единый скрипт, который автоматически выберет лучшую стратегию:**

```bash
./quick-deploy.sh
```

Этот скрипт:
- ✅ Автоматически определяет, есть ли Docker локально
- ✅ Если есть - собирает локально и загружает (быстро!)
- ✅ Если нет - собирает на сервере (медленнее, но работает)
- ✅ Копирует файлы, запускает миграции, проверяет статус

**Деплой только одного сервиса:**
```bash
./quick-deploy.sh api    # Только API
./quick-deploy.sh web     # Только фронтенд
./quick-deploy.sh all     # Все (по умолчанию)
```

---

### 2. 📦 Локальная сборка + деплой - ~3-7 минут

**Если хотите контролировать процесс вручную:**

```bash
# Шаг 1: Соберите образы локально (использует ваш мощный компьютер)
./build-local.sh

# Шаг 2: Задеплойте готовые образы (быстро, без сборки на сервере)
./deploy-local-images.sh
```

**Преимущества:**
- ✅ Не нагружает сервер
- ✅ Быстрее (ваш компьютер мощнее)
- ✅ Можно собрать заранее
- ✅ Меньше риск перегрузки сервера

**Недостатки:**
- ❌ Нужен Docker на локальной машине
- ❌ Нужно ~5GB свободного места

---

### 3. 🐌 Полный деплой на сервере - ~10-20 минут

**Если нет Docker локально или нужна полная пересборка:**

```bash
./deploy.sh production
```

**Преимущества:**
- ✅ Работает без Docker локально
- ✅ Всегда актуальные зависимости

**Недостатки:**
- ❌ Долго (сборка на слабом сервере)
- ❌ Может перегрузить сервер
- ❌ Больше риск ошибок

---

## 📊 Сравнение методов

| Метод | Время | Нагрузка на сервер | Требования |
|-------|-------|-------------------|------------|
| `quick-deploy.sh` | 2-5 мин | Низкая | Docker локально (опционально) |
| `build-local.sh` + `deploy-local-images.sh` | 3-7 мин | Минимальная | Docker локально |
| `deploy.sh` | 10-20 мин | Высокая | Только SSH |

---

## 🎯 Рекомендуемый workflow

### Для ежедневной разработки:

```bash
# 1. Внесли изменения в код
git add .
git commit -m "feat: new feature"

# 2. Быстрый деплой
./quick-deploy.sh

# Готово! Сайт обновлен за 2-5 минут
```

### Для больших изменений:

```bash
# 1. Соберите локально (можно сделать заранее)
./build-local.sh

# 2. Протестируйте локально (опционально)
docker-compose up

# 3. Задеплойте
./deploy-local-images.sh
```

### Для изменений только в API:

```bash
# Деплой только API (быстрее!)
./quick-deploy.sh api
```

### Для изменений только во фронтенде:

```bash
# Деплой только фронтенда (еще быстрее!)
./quick-deploy.sh web
```

---

## 🔧 Что происходит при деплое?

1. **Копирование файлов** - код и конфиги на сервер
2. **Сборка образов** (если нужно) - локально или на сервере
3. **Остановка старых контейнеров** - graceful shutdown
4. **Запуск новых контейнеров** - с новым кодом
5. **Миграции БД** - автоматическое обновление схемы
6. **Проверка здоровья** - убеждаемся что все работает

---

## ⚡ Оптимизации для еще большей скорости

### 1. Используйте Docker BuildKit кэш

```bash
export DOCKER_BUILDKIT=1
export COMPOSE_DOCKER_CLI_BUILD=1
```

### 2. Деплойте только измененные сервисы

```bash
# Если изменили только API
./quick-deploy.sh api

# Если изменили только фронтенд
./quick-deploy.sh web
```

### 3. Собирайте образы заранее

```bash
# Утром соберите образы
./build-local.sh

# В течение дня деплойте быстро
./deploy-local-images.sh
```

### 4. Используйте параллельную сборку (если есть ресурсы)

В `build-local.sh` можно добавить параллельную сборку, но для 1-CPU сервера это не рекомендуется.

---

## 🐛 Решение проблем

### "Docker is not running"
```bash
# macOS
open -a Docker

# Linux
sudo systemctl start docker
```

### "Permission denied" при SSH
```bash
# Проверьте SSH ключи
ssh-add ~/.ssh/id_rsa

# Или используйте пароль
ssh root@94.241.141.194
```

### "Out of disk space"
```bash
# Очистите Docker на сервере
ssh root@94.241.141.194 "docker system prune -a -f"
```

### "Build failed"
```bash
# Попробуйте полную пересборку
./deploy.sh production
```

---

## 📝 Чеклист перед деплоем

- [ ] Код протестирован локально
- [ ] `.env.production` обновлен (если нужно)
- [ ] Миграции БД готовы (если есть изменения схемы)
- [ ] Docker запущен (если используете локальную сборку)
- [ ] Есть доступ к серверу по SSH

---

## 🎓 Полезные команды

```bash
# Проверить статус контейнеров
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml ps"

# Посмотреть логи
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml logs -f"

# Перезапустить сервис
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml restart api"

# Откатить к предыдущей версии (если есть backup)
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml down && docker-compose -f docker-compose.production.yml.backup up -d"
```

---

## 💡 Советы

1. **Всегда используйте `quick-deploy.sh`** - он автоматически выберет лучший метод
2. **Деплойте только измененные сервисы** - экономит время
3. **Собирайте локально** - быстрее и безопаснее для сервера
4. **Проверяйте логи после деплоя** - убедитесь что все работает
5. **Делайте backup перед большими изменениями** - на всякий случай

---

**Готово! Теперь деплой займет 2-5 минут вместо 10-20! 🚀**



