# 🤖 Настройка токенов ботов

## 📋 Какие токены нужны?

У вас **2 бота**:
1. **Админский бот** (`ADMIN_BOT_TOKEN`) - для админов
2. **Клиентский бот** (`CUSTOMER_BOT_TOKEN`) - для клиентов (Python бот)

## ✅ Рекомендуемая настройка

### Вариант 1: Только админский бот (рекомендуется)

```env
# Токен админского бота (обязательно)
ADMIN_BOT_TOKEN=7123456789:AABBccDDeeFFggHHiiJJkkLLmmNNooOPpp

# Можно продублировать или оставить пустым
BOT_TOKEN=7123456789:AABBccDDeeFFggHHiiJJkkLLmmNNooOPpp

# Клиентский бот (не используется в коде, можно оставить пустым)
CUSTOMER_BOT_TOKEN=

# Список Telegram ID администраторов
ADMIN_WHITELIST=83146995,123456789
```

### Вариант 2: Разные токены

```env
# Админский бот
ADMIN_BOT_TOKEN=7123456789:AABBccDDeeFFggHHiiJJkkLLmmNNooOPpp

# Основной бот (fallback, если ADMIN_BOT_TOKEN не указан)
BOT_TOKEN=7987654321:ZZYYxxWWvvUUttSSrrQQppOOnnMMllKK

# Клиентский бот (не используется в коде)
CUSTOMER_BOT_TOKEN=
```

## 🔍 Как это работает?

### Валидация initData (Telegram WebApp)

1. **Приоритет 1:** Используется `ADMIN_BOT_TOKEN` (если указан)
2. **Приоритет 2:** Используется `BOT_TOKEN` (если `ADMIN_BOT_TOKEN` не указан)
3. **Ошибка:** Если оба пустые (кроме development режима)

### Python боты

Python боты (`customer_bot.py` и `admin_bot.py`) используют свои токены напрямую, не через переменные окружения API.

## 💡 Рекомендации

### ✅ Делайте так:

```env
# 1. Укажите токен админского бота
ADMIN_BOT_TOKEN=ваш_токен_админского_бота

# 2. Продублируйте его в BOT_TOKEN (для совместимости)
BOT_TOKEN=ваш_токен_админского_бота

# 3. Оставьте CUSTOMER_BOT_TOKEN пустым (не используется)
CUSTOMER_BOT_TOKEN=

# 4. Укажите список админов
ADMIN_WHITELIST=83146995
```

### ❌ Не делайте так:

```env
# Не оставляйте оба токена пустыми в production!
ADMIN_BOT_TOKEN=
BOT_TOKEN=
```

## 🚀 Быстрая настройка

Если у вас уже есть админский бот:

1. Получите токен от @BotFather
2. Добавьте в `.env.production`:

```env
ADMIN_BOT_TOKEN=ваш_токен
BOT_TOKEN=ваш_токен  # Продублируйте для совместимости
CUSTOMER_BOT_TOKEN=   # Оставьте пустым
ADMIN_WHITELIST=ваш_telegram_id
```

3. Задеплойте:
```bash
./quick-deploy.sh
```

## 📝 Пример полного .env

```env
# ============================================
# TELEGRAM CONFIGURATION
# ============================================
# Токен админского бота (обязательно)
ADMIN_BOT_TOKEN=7123456789:AABBccDDeeFFggHHiiJJkkLLmmNNooOPpp

# Основной бот токен (можно продублировать ADMIN_BOT_TOKEN)
BOT_TOKEN=7123456789:AABBccDDeeFFggHHiiJJkkLLmmNNooOPpp

# Клиентский бот токен (не используется в коде, можно оставить пустым)
CUSTOMER_BOT_TOKEN=

# Список Telegram ID администраторов (через запятую)
ADMIN_WHITELIST=83146995,123456789

# URL Python ботов (для локальной разработки)
CUSTOMER_BOT_API_URL=
ADMIN_BOT_API_URL=
```

## ❓ FAQ

**Q: Можно ли использовать только BOT_TOKEN без ADMIN_BOT_TOKEN?**  
A: Да, но рекомендуется использовать `ADMIN_BOT_TOKEN` для ясности.

**Q: Зачем нужен BOT_TOKEN, если есть ADMIN_BOT_TOKEN?**  
A: `BOT_TOKEN` используется как fallback, если `ADMIN_BOT_TOKEN` не указан. Можно продублировать `ADMIN_BOT_TOKEN` в `BOT_TOKEN`.

**Q: Используется ли CUSTOMER_BOT_TOKEN в коде?**  
A: Нет, не используется. Можно оставить пустым.

**Q: Как получить токен бота?**  
A: Через @BotFather в Telegram:
1. Откройте @BotFather
2. Отправьте `/newbot` или `/mybots`
3. Выберите бота
4. Скопируйте токен

## 🔐 Безопасность

- ✅ Никогда не коммитьте токены в Git
- ✅ Используйте `.env.production` на сервере
- ✅ Регулярно обновляйте токены
- ✅ Ограничьте доступ к `.env` файлу (chmod 600)

