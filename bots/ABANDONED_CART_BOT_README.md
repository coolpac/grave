# 🤖 Abandoned Cart Bot - Автоматические напоминания о брошенных корзинах

## Описание

Отдельный Python бот для автоматической отправки напоминаний клиентам о брошенных корзинах. Работает как фоновый сервис, который периодически проверяет брошенные корзины и отправляет напоминания через Customer Bot.

## Возможности

- ✅ Автоматическая проверка брошенных корзин по расписанию
- ✅ Отправка напоминаний через Customer Bot
- ✅ Учет настроек из админки (включено/выключено, интервал, лимит)
- ✅ Проверка интервала между напоминаниями
- ✅ Ограничение максимального количества напоминаний
- ✅ Логирование всех операций

## Установка

```bash
cd bots
pip install -r requirements.txt
```

## Настройка

### Переменные окружения

Создайте файл `.env` в папке `bots/`:

```env
# URL NestJS API
API_URL=http://localhost:3000/api

# URL Customer Bot (для отправки напоминаний)
CUSTOMER_BOT_API_URL=http://localhost:8001

# Интервал проверки брошенных корзин (часы)
ABANDONED_CART_CHECK_INTERVAL_HOURS=1

# Интервал между напоминаниями (часы) - используется если нет настроек в БД
REMINDER_INTERVAL_HOURS=24

# Максимальное количество напоминаний - используется если нет настроек в БД
MAX_REMINDERS=3
```

## Запуск

### Ручной запуск

```bash
python3 abandoned_cart_bot.py
```

### Через скрипт запуска всех ботов

```bash
./start_bots.sh
```

Скрипт автоматически запустит:
- Customer Bot (порт 8001)
- Admin Bot (порт 8002)
- Abandoned Cart Bot (фоновый процесс)

## Как это работает

1. **Проверка настроек:**
   - Бот запрашивает настройки из API (`/admin/abandoned-carts/settings`)
   - Если автоматические напоминания выключены, пропускает проверку

2. **Получение брошенных корзин:**
   - Запрашивает список брошенных корзин из API (`/admin/abandoned-carts`)
   - Фильтрует восстановленные корзины

3. **Проверка условий:**
   - Пропускает корзины без `telegramId`
   - Проверяет, не превышен ли лимит напоминаний
   - Проверяет интервал с последнего напоминания

4. **Отправка напоминаний:**
   - Получает детали корзины из API (`/admin/abandoned-carts/:id/details`)
   - Отправляет напоминание через Customer Bot (`/notify/abandoned-cart`)
   - Отмечает напоминание как отправленное в API (`/admin/abandoned-carts/:id/mark-reminder-sent`)

## Расписание

По умолчанию проверка выполняется **каждый час**.

Можно изменить через переменную окружения:
```env
ABANDONED_CART_CHECK_INTERVAL_HOURS=2  # Каждые 2 часа
```

## Логирование

Бот логирует все операции:
- Проверка настроек
- Количество найденных корзин
- Успешные отправки напоминаний
- Ошибки при отправке

Пример логов:
```
2024-01-01 12:00:00 - INFO - Checking abandoned carts for reminders...
2024-01-01 12:00:01 - INFO - Found 5 abandoned carts
2024-01-01 12:00:02 - INFO - Reminder sent successfully for cart 123
2024-01-01 12:00:03 - INFO - Reminders sent: 3
```

## Управление через админку

В админке (`/abandoned-carts`) можно:
- Включить/выключить автоматические напоминания
- Настроить интервал между напоминаниями (часы)
- Настроить максимальное количество напоминаний

Настройки сохраняются в БД и автоматически применяются ботом при следующей проверке.

## API Endpoints (используемые ботом)

### GET /admin/abandoned-carts/settings
Получить настройки автоматических напоминаний

**Ответ:**
```json
{
  "id": 1,
  "autoRemindersEnabled": true,
  "reminderIntervalHours": 24,
  "maxReminders": 3,
  "updatedAt": "2024-01-01T12:00:00Z"
}
```

### GET /admin/abandoned-carts
Получить список брошенных корзин

### GET /admin/abandoned-carts/:id/details
Получить детали корзины для отправки напоминания

**Ответ:**
```json
{
  "id": 1,
  "cartId": 123,
  "telegramId": "123456789",
  "itemsText": "  • Товар 1 - 2 шт. × 5 000 ₽\n  • Товар 2 - 1 шт. × 3 000 ₽",
  "totalAmount": 13000,
  "daysSinceAbandoned": 1,
  "reminderSent": 0,
  "lastReminderAt": null
}
```

### POST /admin/abandoned-carts/:id/mark-reminder-sent
Отметить напоминание как отправленное

## Интеграция с Customer Bot

Бот отправляет напоминания через Customer Bot endpoint:
- `POST /notify/abandoned-cart`

**Payload:**
```json
{
  "telegramId": "123456789",
  "cartId": 123,
  "items": "  • Товар 1 - 2 шт. × 5 000 ₽",
  "totalAmount": 10000,
  "daysSinceAbandoned": 1
}
```

## Обработка ошибок

- **API недоступен:** Логируется ошибка, проверка повторяется при следующем запуске
- **Customer Bot недоступен:** Логируется ошибка, напоминание не отправляется
- **Пользователь заблокировал бота (403):** Логируется предупреждение, не критично

## Мониторинг

Рекомендуется мониторить логи бота для отслеживания:
- Количества отправленных напоминаний
- Ошибок при отправке
- Доступности API и Customer Bot

## Остановка

Для остановки бота используйте `Ctrl+C` или найдите процесс и завершите его:

```bash
# Найти процесс
ps aux | grep abandoned_cart_bot

# Завершить процесс
kill <PID>
```

Или если запущен через `start_bots.sh`:
```bash
kill $ABANDONED_CART_PID
```

