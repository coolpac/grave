# Telegram Боты v2.0 🤖

Улучшенная система Telegram ботов для магазина ОптМрамор.

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    TELEGRAM BOTS v2.0                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │ Customer Bot │    │  Admin Bot   │    │ Cart Bot     │  │
│  │   :8001      │    │    :8002     │    │   :8003      │  │
│  │              │    │              │    │              │  │
│  │ • Заказы     │    │ • Новые      │    │ • Авто-      │  │
│  │ • Статусы    │    │   заказы     │    │   напомин.   │  │
│  │ • Корзина    │    │ • Управление │    │ • Scheduler  │  │
│  │ • Меню       │    │ • Статистика │    │              │  │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘  │
│         │                   │                   │          │
│         └───────────────────┼───────────────────┘          │
│                             │                              │
│                     ┌───────▼───────┐                      │
│                     │   NestJS API  │                      │
│                     │    :3000      │                      │
│                     └───────────────┘                      │
│                                                            │
└─────────────────────────────────────────────────────────────┘
```

## Функции ботов

### Customer Bot (порт 8001)
- ✅ Уведомления о новых заказах с inline-кнопками
- ✅ Обновления статуса заказа
- ✅ Напоминания о брошенной корзине
- ✅ Интерактивное меню с Web App
- ✅ Deep links для быстрого доступа
- ✅ Callback кнопки для навигации

### Admin Bot (порт 8002)
- ✅ Мгновенные уведомления о новых заказах
- ✅ Управление статусами прямо из Telegram
- ✅ Просмотр деталей заказа
- ✅ Быстрый доступ к телефону клиента
- ✅ Статистика продаж
- ✅ Whitelist администраторов

### Abandoned Cart Bot (порт 8003)
- ✅ Автоматическая проверка брошенных корзин
- ✅ Настраиваемые интервалы напоминаний
- ✅ Лимит количества напоминаний
- ✅ Интеграция с Customer Bot

## Быстрый старт

### 1. Создание ботов в Telegram

```bash
# 1. Откройте @BotFather
# 2. /newbot -> Customer Bot Name -> customer_bot_username
# 3. Скопируйте токен
# 4. Повторите для Admin Bot
```

### 2. Настройка окружения

```bash
cd bots
cp .env.example .env
# Отредактируйте .env и добавьте токены
```

### 3. Локальный запуск

```bash
chmod +x start_bots_v2.sh
./start_bots_v2.sh
```

### 4. Docker запуск

```bash
cd ..
docker-compose -f docker-compose.production.yml up -d customer-bot admin-bot abandoned-cart-bot
```

## API Endpoints

### Customer Bot API

| Endpoint | Method | Описание |
|----------|--------|----------|
| `/health` | GET | Проверка здоровья |
| `/notify/customer` | POST | Уведомление о заказе |
| `/notify/status` | POST | Обновление статуса |
| `/notify/abandoned-cart` | POST | Напоминание о корзине |
| `/notify/custom` | POST | Кастомное уведомление |

### Admin Bot API

| Endpoint | Method | Описание |
|----------|--------|----------|
| `/health` | GET | Проверка здоровья |
| `/notify/admin` | POST | Уведомление о заказе |
| `/notify/status` | POST | Изменение статуса |

### Abandoned Cart Bot API

| Endpoint | Method | Описание |
|----------|--------|----------|
| `/health` | GET | Проверка здоровья |
| `/trigger` | POST | Ручной запуск проверки |
| `/stats` | GET | Статистика работы |

## Примеры запросов

### Уведомление клиенту о заказе

```bash
curl -X POST http://localhost:8001/notify/customer \
  -H "Content-Type: application/json" \
  -d '{
    "telegramId": "123456789",
    "orderNumber": "ORD-001",
    "customerName": "Иван",
    "total": 15000
  }'
```

### Уведомление админу

```bash
curl -X POST http://localhost:8002/notify/admin \
  -H "Content-Type: application/json" \
  -d '{
    "orderNumber": "ORD-001",
    "customerName": "Иван Иванов",
    "customerPhone": "+7999123456",
    "items": "• Памятник гранитный × 1",
    "total": 15000
  }'
```

## Настройка BotFather

После создания бота выполните команды:

```
/setdescription - Установить описание бота
/setabouttext - Установить текст "О боте"
/setuserpic - Установить аватар
/setcommands - Установить команды меню:

start - Главное меню
help - Справка
orders - Мои заказы
contacts - Контакты
```

### Включение Web App меню

```
/setmenubutton
# Выберите бота
# Укажите URL: https://optmramor.ru
# Укажите текст: 🛍️ Каталог
```

## Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `CUSTOMER_BOT_TOKEN` | Токен Customer Bot | `123:ABC...` |
| `ADMIN_BOT_TOKEN` | Токен Admin Bot | `456:DEF...` |
| `ADMIN_CHAT_ID` | Telegram ID админа | `123456789` |
| `ADMIN_WHITELIST` | Список ID админов | `123,456,789` |
| `WEBAPP_URL` | URL мини-приложения | `https://optmramor.ru` |
| `API_URL` | URL бэкенд API | `http://api:3000/api` |
| `USE_WEBHOOK` | Использовать webhook | `false` |

## Мониторинг

### Health Check

```bash
# Customer Bot
curl http://localhost:8001/health

# Admin Bot  
curl http://localhost:8002/health

# Abandoned Cart Bot
curl http://localhost:8003/health
```

### Логи

```bash
# Локально
tail -f logs/customer_bot.log

# Docker
docker logs -f ritual_customer_bot
docker logs -f ritual_admin_bot
docker logs -f ritual_abandoned_cart_bot
```

## Troubleshooting

### Бот не отвечает
1. Проверьте токен в `.env`
2. Проверьте health endpoint
3. Проверьте логи

### Уведомления не приходят
1. Убедитесь что telegram_id правильный
2. Проверьте что пользователь запустил бота (/start)
3. Проверьте что бот не заблокирован

### Ошибка 403 при отправке
Пользователь заблокировал бота или не запускал его.

## Технологии

- **Python 3.11+**
- **python-telegram-bot 21.x** - Telegram Bot API
- **FastAPI** - REST API фреймворк
- **uvicorn** - ASGI сервер
- **APScheduler** - Планировщик задач
- **aiohttp** - Async HTTP клиент
- **Pydantic** - Валидация данных

## Обновление с v1

Новые файлы:
- `customer_bot_v2.py` (заменяет `customer_bot.py`)
- `admin_bot_v2.py` (заменяет `admin_bot.py`)
- `abandoned_cart_bot_v2.py` (заменяет `abandoned_cart_bot.py`)
- `requirements_v2.txt` (новые зависимости)
- `start_bots_v2.sh` (новый скрипт запуска)

Старые файлы сохранены для совместимости.
