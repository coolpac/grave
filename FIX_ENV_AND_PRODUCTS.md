# 🔧 Исправление .env и создание товаров на сервере

Это руководство поможет вам:
1. Исправить `.env.production` на сервере
2. Создать товары на продакшене

---

## 🔧 Шаг 1: Исправление .env на сервере

### Проблема
API не запускается из-за ошибки: `"CUSTOMER_BOT_TOKEN" is not allowed to be empty`

### Решение

#### Вариант 1: Автоматическое исправление (рекомендуется)

```bash
./fix-env-on-server.sh
```

Скрипт автоматически:
- ✅ Создаст резервную копию `.env.production`
- ✅ Установит пустые значения для опциональных токенов
- ✅ Перезапустит контейнеры
- ✅ Покажет статус и логи

#### Вариант 2: Ручное исправление через SSH

```bash
# 1. Подключитесь к серверу
ssh root@94.241.141.194

# 2. Перейдите в директорию проекта
cd /opt/ritual-app

# 3. Создайте резервную копию
cp .env.production .env.production.backup

# 4. Откройте файл для редактирования
nano .env.production

# 5. Найдите и исправьте следующие строки:
#    Если CUSTOMER_BOT_TOKEN не установлен, добавьте:
CUSTOMER_BOT_TOKEN=

#    Если ADMIN_BOT_TOKEN не установлен, добавьте:
ADMIN_BOT_TOKEN=

# 6. Сохраните файл (в nano: Ctrl+O, Enter, Ctrl+X)

# 7. Перезапустите контейнеры
docker-compose -f docker-compose.production.yml down
docker-compose -f docker-compose.production.yml up -d

# 8. Проверьте статус
docker-compose -f docker-compose.production.yml ps
docker-compose -f docker-compose.production.yml logs api --tail 20
```

#### Вариант 3: Исправление одной командой через SSH

```bash
ssh root@94.241.141.194 << 'EOF'
cd /opt/ritual-app
cp .env.production .env.production.backup
echo "CUSTOMER_BOT_TOKEN=" >> .env.production
echo "ADMIN_BOT_TOKEN=" >> .env.production
docker-compose -f docker-compose.production.yml restart api
docker-compose -f docker-compose.production.yml logs api --tail 20
EOF
```

---

## 📦 Шаг 2: Создание товаров на продакшене

### Подготовка

#### 1. Убедитесь, что категории созданы

Перед созданием товаров убедитесь, что все категории уже созданы:

```bash
# Проверьте через админ-панель: https://optmramor.ru/admin/categories
# Или создайте категории через скрипт (если еще не созданы)
```

#### 2. Получите токен администратора

**Способ 1: Через админ-панель (рекомендуется)**

1. Откройте админ-панель: `https://optmramor.ru/admin`
2. Войдите через Telegram (используйте Telegram WebApp)
3. Откройте консоль браузера (F12 → Console)
4. Выполните команду:
   ```javascript
   localStorage.getItem('authToken')
   ```
5. Скопируйте полученный токен

**Способ 2: Через API (если есть доступ)**

```bash
# Если у вас есть Telegram initData
curl -X POST https://optmramor.ru/api/auth/validate \
  -H "Content-Type: application/json" \
  -d '{"initData": "YOUR_TELEGRAM_INIT_DATA"}'
```

### Создание товаров

#### Вариант 1: Автоматический скрипт (рекомендуется)

```bash
./create-products-on-server.sh
```

Скрипт запросит токен и создаст все товары.

#### Вариант 2: Интерактивный режим

```bash
cd apps/api

# Запустите скрипт в интерактивном режиме
npx tsx scripts/create-products-production.ts --interactive
```

Скрипт запросит:
- URL API: `https://optmramor.ru/api`
- Токен авторизации: (вставьте токен из админ-панели)
- Подтверждение: `yes`

#### Вариант 3: Через переменные окружения

```bash
cd apps/api

# Установите переменные окружения
export API_URL=https://optmramor.ru/api
export AUTH_TOKEN=ваш_токен_здесь

# Запустите скрипт
npx tsx scripts/create-products-production.ts
```

#### Вариант 4: Одной командой

```bash
cd apps/api
API_URL=https://optmramor.ru/api AUTH_TOKEN=ваш_токен npx tsx scripts/create-products-production.ts
```

---

## ✅ Проверка результатов

### 1. Проверьте статус API

```bash
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml ps"
```

Все контейнеры должны быть в статусе `Up` и `healthy`.

### 2. Проверьте логи API

```bash
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml logs api --tail 50"
```

Не должно быть ошибок валидации конфигурации.

### 3. Проверьте товары в админ-панели

1. Откройте: `https://optmramor.ru/admin/products`
2. Убедитесь, что все 9 товаров созданы:
   - Плита мраморная полированная
   - Заказная плита
   - Плитка тротуарная
   - Мраморная брекчия
   - Декоративная мраморная крошка, песок
   - Стела ритуальная
   - Тумба ритуальная
   - Цветник ритуальный
   - Ритуальная ваза

### 4. Проверьте товары на фронтенде

1. Откройте: `https://optmramor.ru`
2. Перейдите в категории
3. Убедитесь, что товары отображаются

---

## 🐛 Решение проблем

### Проблема 1: API не запускается после исправления .env

**Решение:**
```bash
# Проверьте логи
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml logs api"

# Пересоздайте контейнеры
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml down && docker-compose -f docker-compose.production.yml up -d"
```

### Проблема 2: "Категория с slug не найдена"

**Решение:**
1. Убедитесь, что категории созданы через админ-панель
2. Или создайте категории через скрипт:
   ```bash
   ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml exec api npm run create:categories"
   ```

### Проблема 3: "Unauthorized" или "401" при создании товаров

**Решение:**
1. Получите новый токен через админ-панель
2. Убедитесь, что токен скопирован полностью (без пробелов)
3. Проверьте, что токен не истек

### Проблема 4: "Product with slug already exists"

**Решение:**
Товар с таким slug уже существует. Вы можете:
- Удалить существующий товар через админ-панель
- Изменить slug в скрипте
- Пропустить создание этого товара

### Проблема 5: Товары не отображаются на фронтенде

**Решение:**
1. Проверьте, что товары созданы в админ-панели
2. Проверьте, что товары привязаны к правильным категориям
3. Очистите кэш браузера
4. Проверьте логи фронтенда в консоли браузера

---

## 📋 Чек-лист

- [ ] Исправлен `.env.production` на сервере
- [ ] API запущен и работает (статус `Up` и `healthy`)
- [ ] Получен токен администратора
- [ ] Категории созданы
- [ ] Скрипт создания товаров выполнен успешно
- [ ] Товары отображаются в админ-панели
- [ ] Товары отображаются на фронтенде

---

## 💡 Полезные команды

```bash
# Проверка статуса всех сервисов
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml ps"

# Просмотр логов API
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml logs api -f"

# Перезапуск API
ssh root@94.241.141.194 "cd /opt/ritual-app && docker-compose -f docker-compose.production.yml restart api"

# Проверка подключения к API
curl https://optmramor.ru/api/health/live

# Проверка товаров через API
curl -H "Authorization: Bearer YOUR_TOKEN" https://optmramor.ru/api/products
```

---

## 📚 Дополнительные ресурсы

- [CREATE_PRODUCTS_PRODUCTION.md](./CREATE_PRODUCTS_PRODUCTION.md) - Подробное руководство по созданию товаров
- [DEPLOYMENT_QUICK_START.md](./DEPLOYMENT_QUICK_START.md) - Руководство по деплою
- [PRODUCTION_SETUP.md](./PRODUCTION_SETUP.md) - Настройка продакшена



